# FINDINGS

One line each, dated, newest last.

- 2026-09-22 The golden table the C++ port is checked against was seven rows behind the C# Core; the missing rows are the caught-claim ones, and they were left out precisely because the port cannot answer them, so the comparison passed by never being asked. Fixed: the table is regenerated from the Core and the hole is named and counted.
- 2026-09-22 Four of the five suites D16 named as "these keep running" — Soak, SaveChaos, PerceptionGolden, StrangerTest — were in no push check. Their project directories survived the whole time, which is exactly what made the loss invisible.
- 2026-09-22 The Unreal safeguard could not turn a run red: the build step was marked continue-on-error and the last step banked evidence and exited clean, so a failed build reported green.
- 2026-09-22 The two golden comparisons are not equally strict. The g++ one fails on a row it cannot answer; the in-engine one counts it, prints a note and still says PASS. Left as it is for now, written down so the next reader is not surprised by a green engine and a red runner.
- 2026-09-22 The scheduled task "LEDGER supervisor" was still running on this PC, driving the other checkout, which pushes to the same repository.
- 2026-09-22 The commit-message hook in .githooks/ is not wired up (no hooks path set), so it has never refused anything. Left as found; noted so it is not mistaken for a live gate.
- 2026-09-22 The Blender preview workflow the lamp recipe derives its own invocation from is gone (archived with the studio), so one of the recipe selftest checks has been failing on a dead path; a second, the bad-args refusal check, fails too. Both predate this sitting. Recipe passes 71 of 73.
- 2026-09-22 A full cold build, cook and package of the Unreal project takes 4.3 minutes on this PC, and a warm one 1 to 2. That number had never existed; the workflow allows sixty minutes for it.
- 2026-09-22 The independent check earned its place on its first use: given the act gate and the intended behaviour in plain words, an outside reader found that a press left over from an abandoned attempt would be credited to the NEXT crime, that the evidence called the deed committed before it had happened, that keyRouted meant only "a controller exists", that the ceiling constant could be set to zero with every test still green, and that the real gate lived in a file no test compiles. All fixed before the commit.
- 2026-09-22 The crime verdict's witnessStatusNote is a hardcoded string. It printed "w1-filed-on-A-with-a-rung" on a run where nothing was witnessed at all. Not fixed today; named here because it is a sentence the evidence file asserts rather than measures.
- 2026-09-22 Nothing automated reads the crime verdict. A run in which the input path was dead commits and pushes green; only a human reading the file catches it. That gate belongs with the witnessed/control regression pair, which is the next sitting's work.
- 2026-09-22 THE NUMBERS THAT HAD NEVER EXISTED, taken off the commit clock rather than estimated. The whole sitting, from the first file touched to the last push: 40 minutes. The lamp column, finished from its dimensions over two rendered attempts: 6 minutes. The first terrace front, recipe and selftest written from the spec's machine-readable numbers plus two rendered attempts looked at: 4 minutes. The crime encounter, which was the long one: 18 minutes, most of it four Unreal packages. A commit message pushed earlier this sitting says the terrace took about forty minutes; that was the whole sitting and it is wrong.
- 2026-09-22 The first terrace render came back as an empty field, because the camera was aimed a quarter turn the wrong way and measured against the wrong axis. Every other check passed. The aiming is a closed form now and the selftest composes it back and compares it against the direction asked for, on the actual frames the recipe takes.
- 2026-09-22 The Unreal build has not run once since the studio was paused. The probe workflow's second step calls tools/runner/bootstrap-paths.cmd, which went to the archive with the studio's other machinery, so every push since failed on a missing file and skipped the build. Five red runs, all red for a reason that had nothing to do with the build, and the safeguard hardened last sitting was standing behind a door that never opened.
- 2026-09-22 The core tests ran and passed on the one push that touched their paths, with all four restored suites and the regenerated golden table in them. That table is the first evidence the C++ port agrees with a C# golden generated on the same commit rather than with a committed file.
- 2026-09-22 The independent check paid for itself a second time, and harder: given the ported memory reload it compiled BOTH engines and ran 45 identical inputs through them side by side. Four real disagreements, none of which any test would have caught. Leading whitespace before the D in a timestamp: the C# refuses it, the port accepted it. NaN and Infinity as an importance: the C# accepts all three, the port refused them - and the C# engine itself WRITES "(NaN|heard)" into a memory file, so this was a file one engine wrote and the other silently dropped. The smallest possible integer as a day: refused by the port, accepted by the C#. A non-breaking space after a section heading: the C# trims it, the port did not, so every belief under that heading vanished in one engine and not the other. All four fixed.
- 2026-09-22 The same check found four mutations of the port that every one of the nineteen new rows survived, including deleting the guard the original C# is proudest of - searching for the closing bracket from the opening one rather than from the start. The fixture is nineteen lines now and each of those mutations dies on it.
- 2026-09-22 The two halves of a golden fixture can drift apart while both stay green, if the line that changes is one both engines reject. The fixture text is now itself a row, so a drift mismatches before any behaviour does.
- 2026-09-22 The Core suite could not pass on this Windows checkout at all: one test builds a rejecting fixture by cutting a line out of the live scene file matching a bare newline, and the working copy has Windows line endings. It passed in CI the whole time, because the Linux checkout does not. A test that runs on only one of the two machines the project uses is a test the other machine cannot be asked to run.
- 2026-09-22 The two engines write the memory markdown with different line endings - the C# uses the platform's, the port always uses a bare newline. The reload normalises, so nothing breaks today, but the save format is now a file both engines may write and that is worth knowing.
- 2026-09-22 CONFIRMED ON A REAL FAILED BUILD: a line that does not compile was pushed on purpose, the build step failed, the verdict step failed, and the run went red. The commit before it went green in nine minutes. A push does run the checks; a failed build does fail the run. None of it had ever been watched.
- 2026-09-22 THREE CHECKS IN ONE DAY THAT COULD ONLY PASS ON ONE OF THE TWO MACHINES THIS PROJECT USES. The Core suite cut a fixture by a bare newline and this checkout has Windows line endings. The canon gate matched its exemption list with forward slashes against paths that carry backslashes, so canon.md screened itself and the judge's deliberately-wrong fixtures reported sixteen findings. The attribution sweep walked the Unreal build's own output, which is in .gitignore and so has never existed in CI, and called tens of thousands of build artefacts unattributed assets. All three passed in CI the whole time. All three now run on both.
- 2026-09-22 The attribution sweep lost the probe's live output directory when the studio was archived: the row moved to the legacy path and nothing was left for the path the probe still writes to. It went unnoticed for exactly as long as the probe was broken, and went red the first run after it was fixed.
- 2026-09-22 Hue does not carry at distance; value does. The terrace's replacement side door was a blue-green that looks nothing like brick and sat at a luminance of 0.0483 against the brick's 0.0488 - the same door hiding in the same wall wearing a different colour. The recipe measures that now instead of trusting an eye.
- 2026-09-22 THE ENGINE AND THE TEST BENCH DISAGREED ABOUT NaN, and only a real build could show it. Unreal compiles this module with fast floating point, under which the compiler may assume no NaN exists and folds `V != V` to false; the container where the port's tests run does not. The same source answered "NaN" everywhere a test runs and "0.00" inside the game. Detected by its bits now, which no optimiser is allowed to delete.
- 2026-09-22 The two readers of the golden table had different strictness and nobody had noticed. The cheap one FAILS on a row it cannot answer; the in-engine one counted such rows, printed a note, and passed anyway. So a hole opening in the port would have gone red in the cheap check and green in the expensive one - the wrong way round. One list of known holes in CoreGolden.h now, read by both, and an unanswered row fails in both.
- 2026-09-22 "11 row(s) named a function this build does not implement" cost a whole round trip to the PC to find out which eleven. The in-engine reader names them now, up to ten, the way the container's does.
- 2026-09-22 THE NaN DIVERGENCE HAD A SECOND, DEEPER CAUSE, and only a second real build found it. Fixing the formatter was not enough: the value was already gone one call earlier. C# Math.Clamp returns NaN because every comparison against NaN is false, and this port's clamp is written the same way - but under Unreal's fast floating point the compiler may emit it as SSE min/max, whose defined behaviour on a NaN operand is to return the OTHER one. So the engine's clamp turned NaN into 0.0 while every machine the tests run on kept it. The clamps test the bits now.
- 2026-09-22 Making an unanswered golden row fail turned the in-engine run red on a hole that had been there all along and was ruled in advance: FactNull's four rows need exceptions and the Unreal module compiles without them, which Build.cs says in as many words. It had been counted and ignored for weeks. It is named now, keyed to the compile flag rather than to a list, so the container - which has exceptions - still answers all four.
- 2026-09-22 A run where every step passed, build and verdict included, went red because its evidence push lost a race with mine. Three retries over fifteen seconds is not a window; six over two and a half minutes is. A job that reports red for a reason nobody can distinguish from a broken build is worse than one that reports nothing.
- 2026-09-22 The elevation camera stands forty-one metres back to get six bays in frame, and the footway ran fourteen metres out from the frontage, so the pavement was BETWEEN the camera and the building and drew a grey band across the bottom of every elevation - over the shopfronts, which are the thing being judged.
- 2026-09-22 Soot-darkened grey stock brick is LIGHTER than red brick in daylight, not darker. At the value first chosen the whole west row read as a black slab and the only thing on it that carried was the doors.
- 2026-09-22 The street rendered from the sheet's own viewpoint for the first time, and the first thing it showed is that NOTHING IN IT SET SCALE: no object whose size a person already knows. A door is 1.981 m and a kerb is 125 mm and the eye measures against neither. Two figures fixed it in twenty lines. D31's own tying frame asks for one in as many words - "a figure in silhouette" - and until today nothing this recipe drew could have been that frame.
- 2026-09-22 The first pair of figures read as two dark posts: their shoulders spanned the street's length and the sheet's viewpoint looks ALONG the street, so the camera saw the 0.25 m side of each. A figure that does not read as a person sets no scale at all.
- 2026-09-22 The lamp column recipe could not be imported inside Blender: it runs itself on load, by a guard that fires on `bpy is not None` as well as on being the main script. Importing it ran its main(), which parsed the STREET recipe's arguments, refused one of them, and killed the process before a frame was drawn. It has a named escape now, so the street can stand the accepted column rather than building a second one.
- 2026-09-22 D31's tying frame exists for the first time: the street at dusk, wet, lamps lit, a figure in silhouette. What it shows next is that the footway reads almost white against a dark street and the lanterns throw almost no pool, so the light is arriving from the sky rather than from the lamps.
- 2026-09-22 THE LANTERNS WERE SEALED INSIDE THEIR OWN HOUSINGS. The piece file's rule is one point light 0.05 m below the centre of each EMISSIVE piece, and the emissive piece is the lens on the lantern's underside; read as the lantern's centre it put the light 0.10 m inside a 0.20 m deep opaque steel box. Two rounds of raising the power changed nothing, because the problem was never the power.
- 2026-09-22 The night frame had a sun in it at 0.10 when the scene file says, in as many words, sun off and sun_intensity 0.0. That was the light arriving from the sky rather than from the lamps, and the spec had been right all along.
- 2026-09-22 overcast_day carries wetness 0.6 and every day frame had been rendered bone dry. The sheet's own street panel is OVERCAST DAYLIGHT and its road is a near mirror, so that was a third of the gap on its own.
- 2026-09-22 OUR FRAME WAS THE MIRROR OF THE REFERENCE and no one could have judged the pair. cam_A stands on the parade's OWN pavement; the sheet stands on the far side with its shops eight to ten metres off on the right. Half of it was which pavement, and the other half was which way along: a camera looking up the street has the east side on its left hand, so the parade came out on the wrong side twice over.
- 2026-09-22 A wet pavement and a wet road are not the same surface. Made alike, the footway behaved like sheet tarmac, and since road and footway fill the bottom third of the frame the whole picture read pale. The sheet has the road as a near mirror with the shopfronts legible in it and the pavement as a dull wet grey.
- 2026-09-22 Our sky was not overexposed by lighting; it had no tone curve at all. Blender's Standard view transform clips everything over 1.0, and the reference is a photograph.
- 2026-09-22 The texture pack we already hold names eighteen surfaces after the very surfaces the scene file names, with base, roughness and normal maps, CC0 from ambientCG and on the allowlist. Nothing had to be fetched.
- 2026-09-22 THE PACK'S NAMES DO NOT MATCH ITS OWN COLOURS. brick_red averages a sandy fawn and brick_grey averages a pinker brown, so taking each map's own hue put the red parade in pale sandstone and the grey west row in red brick - the two rows swapped and only thirteen percent apart in value, which is inside the bound this project's own check refuses. And `roof` is terracotta pantile; this town is slated, and the slate is `roof_b`.
- 2026-09-22 Texture tiles were all about three times too large on the first pass: bricks reading 0.25 m on the course against a real 0.075. A texture at the wrong scale is worse than none, because it does not merely fail to say what the surface is - it says the wrong thing about how big the building is.
- 2026-09-22 Our shop windows were black walls. Glass with no transmission and a very dark base is a mirror with a tint, so every frontage on the row read as a boarded hole - the opposite of what a window is, which is the one surface on a facade you are meant to see past.
- 2026-09-22 Lighting the shops INVERTED one of this recipe's own checks and the check caught it. While the glazing stood for an unlit interior the joinery had to be the lighter of the two or no frame drew; with the window lit, a British shopfront's joinery is dark read against the brightness. Same requirement, opposite direction, and the old wording would have quietly passed a frontage with no frame at all.
- 2026-09-22 Depth beyond thirty metres is still open. The scene file's fog figures have never been applied to anything, and a world volume scatter rendered pure black twice - at the file's own density, and again with EEVEE's volumetric range opened to 120 m, more samples and a third of the density. Mean frame brightness 0.1 out of 255 both times. Named rather than pursued; the next attempt does not start from scratch.
