line: tools and channel (tools/producer-check.py, the walked trees)
spec: Three frozen name lists now live in one tool, each waiving a NAMED
  subset of the register for files that were correct under the rulebook in
  force when they went: LEGACY_LINK_RULES (3 names, 2026-09-06),
  RESEARCH_VERBATIM (10, 2026-09-14) and PRE_MOVE_MESSAGES (40, 2026-09-15).
  The third was ruled the right shape for its day (ruling of 2026-09-15
  05:43Z, section 3) and the line was drawn in writing at a fourth. The
  structural fact under all three: production/outbox/ and production/briefs/
  keep every sent message by rule (the outbox README: "a sent message stays
  here"), the gate re-grades all of them on every commit, so every change to
  the rulebook re-grades an archive, and each time the answer has been a
  list of names. Queue 077 met the same fact for the clock by pinning each
  file to its own filename date, and refused a machine-written sent marker
  as "a flag that can be set wrongly".
  THE QUESTION THIS ITEM ANSWERS, AND DOES NOT ASSUME: whether the rulebook
  generation a sent file is graded under should be selected by its RECEIPT
  (production/outbound/<name>.receipt.txt, written by the sender at send
  time, carrying messageId and the send instant), which the specimen cannot
  choose by naming itself, so the three lists collapse to one rule plus a
  short list for receipt-less files (today: briefs 2026-09-06, -07 and -12).
  Trade-offs to weigh in writing before any code: a receipt is a second tree
  the gate would read; a forged receipt is a false sentence of the same
  class as a false ruling stamp, which the cadence gate already accepts as
  its residual hole; the tool's own text says a date selects no rule, and a
  send instant is a date that is not the specimen's, so that sentence would
  need to say which dates it means.
acceptance: a written answer either way, in the tool's own comment block and
  in a ruling. If yes: the gate prints one generation per file with its
  source (receipt or list), the three lists shrink to the receipt-less
  names, both outcomes on planted trees, accepting first, and the live done
  line's counts quoted before and after. If no: the reason, and the rule
  that a fourth frozen list needs a ruling first, stated where the third one
  is.
max_sessions: 1
status: READY 2026-09-15, filed by the ruling of 05:43Z, section 3. Studio
  work under the spend rule of 2026-09-14 (production/stages.md, two thirds
  of the week to game work), so it waits behind the visual slice and is not
  this week's unless the share allows. Nothing is red while it waits: the
  third list holds the gate green with its counts printed on every run.
