line: process (the cadence gate's scope table in ledger/verify.py)
spec: D45 says the tools that measure the game get a test and no review. The
  cadence gate gates ue-probe WHOLESALE, because that prefix holds the Unreal
  port of the Core and the moat, pinned by a golden file. So a pure instrument
  fix inside ue-probe takes a full director review that D45 says it should not
  need, and the gate cannot tell the two apart because NO PATH CARRIES THE
  DISTINCTION.

  IT FIRED TWICE IN ONE NIGHT ON EXACTLY THAT SHAPE, 2026-09-16: queue 326 is
  an instrument fix in Public/FrameStats.h and it took a director; the same
  question was noted earlier for a pure grade pass under D41.

  AND THE DIRECTOR IT FIRED ON REFUSED TO MOVE IT, which is the reason this is
  a card and not a fix. Its words in section 8: it will not move a gate inside
  the ruling that gate fired on. Its measurement for that refusal is worth
  keeping: five findings per 769 lines, four of them invisible from any
  verdict, is the reading that says the review under this prefix still earns
  its spawn. verify.py's own scope table already refuses a tests/ carve-out
  with a D41 reason, that ue-probe/tests/ holds the Core's planted rejecting
  cases.

  RESEARCH FIRST AND NO SCOPE CHANGE WITHOUT IT. Print, per landed commit over
  the last 200, the ue-probe lines split by tests/, Public/ and Private/, and
  by whether the file is in the Core port's list. Propose a directory split
  from that series and not from an argument. This is rule 2 applied to a
  process bound: a gate scope set without a printed series is a rounding
  wearing a measurement's clothes.
acceptance: the series printed and read; a proposal that names which
  directories move and what each would have gated over those 200 commits, with
  the count of batches that would have changed side; and a DIRECTOR, because
  this is a change to the commit gate itself and the gate is mechanical by
  design and never judged. If the series says the split does not pay, that is
  a finding and the item closes on it.
max_sessions: 1
status: READY 2026-09-16, filed by the 22:25Z ruling's section 10 as item C.
  Lowest of the three and deliberately so: it costs reviews rather than
  correctness, and tonight's evidence is that those reviews are finding real
  faults. Not to be taken while anything in 325, 329, 330, 319 or 324 is open.
