line: instruments (the figure's silhouette reading; queue 372's first step)
spec: DICTATED BY THE DIRECTOR, 2026-09-16, as the first step of queue 372 and
  NOT as its answer. 372 asks which neighbourhood the figure should be read
  against. This item refuses to answer it from an argument and prints both
  instead.

  WHAT TO DO: beside the existing ring, computed by padding the box by its
  LONGER side, print a SECOND annulus computed by padding it by its SHORTER
  side, on the same token, from the first real figure frames. NEITHER DECIDES
  ANYTHING. The silhouette verdict keeps reading the first one until a series
  exists to choose from.

  WHY BOTH AND NOT A SWAP. `LampRingPadPx` is shared with the lantern, whose
  night series is already measured against the current pad, so swapping the
  rule silently re-bases the lamp's readings too. Printing a second number
  costs nothing and changes no existing reading.

  THE BIAS TO WATCH, named by the director before any frame exists: on the
  test fixture the longer-side ring is 15480 over 1080, about 14 to 1; on a
  real night frame it is nearer 20 to 1, because it reaches sky above the head
  and footway below. AT NIGHT THE DARK SKY DRAGS THE RING MEAN DOWN, which
  biases toward a FALSE NO on a figure that really is a silhouette. That is the
  expensive direction and it is worth knowing before the first line is read.
acceptance: both annuli print on the same figure token with their pixel counts,
  over real night frames, and the record states which way they differ and by
  how much; no verdict changes in this item
max_sessions: 1
status: READY 2026-09-16, filed and NOT started. It needs frames with a figure
  in them, and none exist yet.
