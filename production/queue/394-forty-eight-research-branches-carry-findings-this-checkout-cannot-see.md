line: docs (research lane consolidation; closes queue 387's evidence gap)
spec: Jafar, 2026-09-21 ("THE CLEANUPS"): "Every research delivery sits on
  its own branch and none of them is in this checkout... Copy every
  SUMMARY.md, RECHECK.md and BRIEF.md onto main under one folder, keeping
  the overturn markers the lane added. Write one page above them: every
  recommendation sorted into four columns, ruled by me with the record
  named, mine to rule with the question phrased for a one-line answer, a
  correction the studio applies under D43 with its queue item, and a fact
  to check with what would check it. The ladder references that page, and
  queue 387's note about the missing evidence is closed. Delete or merge
  each research branch once its files are on main."

  MEASURED AND GIVEN, per the order that commissioned this item (12:3xZ,
  quoted in production/NOW.md): `git ls-remote --heads origin` returns 52
  heads, 48 of them research/*; `git branch -r --merged origin/main`
  returns 0 research branches merged. Re-run both at pickup time, since
  branches move; state the fresh counts rather than quoting these as
  current.

  This also closes production/queue/387's open note, "the meridian-test-
  administration delivery was not in this checkout... that reading
  stands," and the NOW.md correction under D43 that the delivery "is not
  missing, it is unmerged." Once this item lands, re-read and update queue
  387 to say so rather than leave it describing a now-stale gap.
acceptance: every SUMMARY.md, RECHECK.md and BRIEF.md found across the
  research/* branches, count stated fresh at pickup time, is copied onto
  main under one named folder, with each file's overturn markers intact,
  spot check a sample and state how many were checked and how many
  carried a marker; a single page above them sorts every recommendation
  found into exactly the four named columns, ruled-with-record,
  his-to-rule-as-one-line-question, studio-correction-with-queue-item, and
  fact-to-check-with-what-checks-it, with a count of how many
  recommendations landed in each column; `git ls-remote --heads origin`
  and `git branch -r --merged origin/main` are re-run at close and both
  counts printed, naming any branches still unmerged; and queue 387 is
  updated to point at this item rather than left describing the
  pre-consolidation state
max_sessions: 3
status: READY 2026-09-21, filed as studio-third work, taken at a
  checkpoint inside the studio's third of the week's spend. Not gated on
  the visual slice or the measurements, it is cleanup rather than game
  work, but per his ordering the game two-thirds is spent first, so pick
  this up at a checkpoint rather than ahead of slice or measurement work.
  This is this week's work.
