line: instruments (the measurement half split out of queue 405 by ruling)
spec: SPLIT OUT OF QUEUE 405 BY THE DIRECTOR, 2026-09-21, and it is the half
  that may proceed. 405 is BLOCKED on Jafar's decision about what reach the
  moat requires. THIS item is the measurement that has to exist before that
  question is worth putting to him at all.

  THE DIRECTOR'S REASON, and it is the whole point: "THE CARD DOES NOT GO TO
  HIM BEFORE THAT SERIES IS PRINTED: a bare 'twenty people' invites a constant
  to be tuned."

  WHAT THE SOAK MEASURED AND WHAT IT DID NOT. It measured that residents who
  ever remember anything are FLAT AT 15 TO 22 at every population from 7 to
  500, with `deepestHopEver=2`. What was first written as a property of two
  constants is not: the hop test is `passed = Confidence * tie * HopDecay`
  (`Gossip.cs:410`), so THE CAP IS TIES TIMES DECAY. At tie 1.0 the same
  constants carry a rumour SEVEN transfers; at 0.5, one. So the two-hop reading
  describes WHERE THE SOAK'S TIE WEIGHTS SIT, and those are resampled from the
  eleven authored ties (`Soak/Program.cs:729-730, 794`).

  THE THREE NUMBERS THAT MUST PRINT, and none of them is printed today:
  1. THE ELEVEN AUTHORED TIE WEIGHTS, as a series and not a mean. A mean tie
     weight cannot answer a question whose mechanism is multiplicative.
  2. THE SEED CONFIDENCE OF A SIGHTING, which sets where every chain starts.
  3. THE HOP CONVENTION: whether a "hop" counts a transfer or a mouth, because
     `deepestHopEver=2` means different things under the two readings and every
     sentence written about reach so far has assumed one without saying which.

  THEN THE LEVER SERIES: reach against each lever, ONE LEVER AT A TIME at a
  fixed population, so the relationship between a constant and the number of
  people who end up knowing is READ rather than guessed. The levers are public
  fields at `Gossip.cs:141-142` plus the half-life in `Age`.

  THE CORE SHIPS NOTHING FOR THIS. The director was explicit: the series is
  PLANTED IN THE SOAK HARNESS through those public fields. A lever moved in the
  Core to make a measurement is a Core change wearing a measurement's clothes.
  It is also rule 5b's other half: a cap-asserting instrument needs one run
  where the cap CAN lift, or it cannot tell a real ceiling from its own.
acceptance: the three numbers above are printed with their denominators and the
  hop convention stated in words on the line; then a reach series against each
  lever separately at one fixed population, printed as a series and not as a
  conclusion; and the Core is unchanged, provable by `git status
  ledger/Assets/Scripts/Core/` being empty at hand-back
max_sessions: 2
status: READY 2026-09-21. Studio measurement under D44 and D45, so no director
  and no ruling record for the work itself. It is NOT this week unless budget
  remains, per his order: the visual slice, the five measurements and the art
  lane come first, and this is none of them.

  DO NOT TUNE A CONSTANT WHILE DOING THIS. The soak is the instrument that
  would report the improvement, and moving a constant and then citing the soak
  is a ratchet. Queue 405 carries the same warning and this item inherits it.

  RULED BY: `game-design/decision-2026-09-21-ruling-five-landings-the-sixth-site-the-settled-night-frame-and-the-neighbourhood-that-is-not-the-town.md`, which split this out of 405.
