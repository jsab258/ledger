# Ruling, 2026-09-15 (05:43Z): the forty are waived by name and the fourth list is the line, the walk-back stands as his number and the 0.368 is withdrawn, the provenance guard checks what exists, three is the done line, and the ruled URL stays with no user

STATUS: LOG, 2026-09-15. NOT CURRENT once run 45 has landed and its frame has
been read in the order of section 11; from then the verdict file, queue 299,
256, 259, 303, 306 and 307 are the reading copies and this is the record of
what was ruled and why.

Director ruling on the batch behind queue 299 (Jafar's 0.85 walk-back and the
D40 comment correction) and queue 256 with 259 (the site links move, the
served-commit marker, the floor coming back on, and the forty messages that
went red with it). Escalated because `director_cadence` reads `DIRECTOR NOT
SPAWNED: 1002 changed line(s) vs 100 threshold`,
`workByScope=ledger:116/tools:479/ueprobe:407`, reference commit `5a8ef789`.
Six calls were put to this director and each is ruled below with the evidence
beside it. No code was written by this director and this director has no
shell: every number below was read off the file and line it names in this
session, or is arithmetic shown in full. Where the resident's own runs are
relied on (the g++ count, the two selftests, the gate's done line) the record
says so and asks for the printed line in the commit message rather than
trusting the report.

Author: tier-1 director, stamp at the foot naming row 628 of
`.claude/agent-log.tsv` (`2026-09-15T05:43:41Z` TAB `studio-director`,
agentId `a5803c5f9b9b91e71`), the last row in the file and later than the
reference `2026-09-15T00:52:56Z` (row 621). Rows 624 to 627
(`engine-specialist` and `instrument-builder`, 05:07Z to 05:29Z) are the
builders' rows and are not claimed. The resident never writes the stamp.

## 0. What was opened

`production/queue/299`, `256`, `259`, `184`, `186` (by grep), `275` (by
name), `303`, `305`, `077` 30 to 54 whole or as stated;
`ledger-v2/respec/decision-register/D40-*.md` whole;
`ledger-v2/respec/decision-register/rulings-log.md` 268 to 298;
`game-design/decision-2026-09-15-ruling-the-grade-lands-*.md` whole as the
model; `game-design/decision-2026-09-09-the-hook-comparison-*.md` 462 to 486
and the grep hits at 311, 372, 412, 427, 470 to 479;
`game-design/decision-2026-09-11-ruling-the-link-floor-marker-batch-*.md`
108 to 121 and 209 to 222; `tools/producer-check.py` 260 to 520, 640 to 880,
1288, 1320 to 1380, 1600 to 1750, 2960 to 3150, 3570 to 3710, 3769 to 3838,
3885 to 3975, 4050 to 4130, 4200 to 4270, 4370 to 4495;
`ledger/verify.py` 1780 to 1970, 3536 to 3565, 6585 to 6714 and the greps
named below; `.github/workflows/publish-glance.yml` whole;
`production/site-served.txt` whole; `production/outbox/README.md` whole;
`tools/map-notify.py` 1 to 59; `tools/docs-check.py` 60 to 350;
`tools/queue-check.py` by grep (37, 38, 75, 86, 249);
`ue-probe/Source/LedgerProbe/Public/SurfaceBind.h` 280 to 690 and 1336 to
1420; `ue-probe/Source/LedgerProbe/Private/VignetteShot.cpp` 155 to 205;
`ue-probe/tests/vignette-spec-test.cpp` 2528 to 2563 and 2690 to 2750;
`production/NOW.md` 1 to 70; `production/stages.md` 280 to 301;
`production/quality-ladder.md` 128 to 135;
`production/outbound/brief-2026-09-15.receipt.txt` whole; the listings of
`production/outbox/` (35 entries), `production/briefs/` (12),
`production/outbound/*.receipt.txt` (76), `production/queue/30*` (6),
`production/wakes/2026-09-15*` (6) with `T0551Z` and `T0233Z` read whole;
`.claude/agent-log.tsv` 618 to 628.

Greps, with their counts: `JafarWalkBack` over the tree, 4 hits, two in
`SurfaceBind.h` (the definition at 415 and the one call at 660), one in
queue 299, one in the 05:51Z wake; `WalkBack|walkback|GradeStrength` over
`ue-probe/`, 12 hits, none inside `ProceduralAlbedoTexel` (501 to 527 read
whole); `served_marker_provenance` over `verify.py`, 2 hits, the definition
and the call at 1910; `printedBy|servedUrl|publish-glance|publishGlance` over
`verify.py`, hits only at 1795 to 1874 and 2314, so no fixture asserts a
`markerProvenance=` refusal; `researchVerbatimWaiverBit` over the tree
excluding the tool, 1 hit, the 2026-09-14 leak ruling reading `4/5`;
`10/10|10 of 10|10 of the 10|ten of ten` over every `.md`, `.txt` and `.py`,
saved and re-grepped for `waiver|researchVerbatim`, 0 hits; `0.368` over the
tree, 2 hits that are this batch's (queue 299 line 309 and the 05:51Z wake
line 9), the rest unrelated fixtures and old runs; `github.io/` over
`tools/`, 3 archive hits (`producer-check.py:671`, `map-notify.py:40`, `:44`)
and every other hit `github.io/ledger/`; `pre_move` over
`producer-check.py`, no hit inside `main()` (4441 to 4492 read);
`producer-check` over `tools/runner/`, `telegram-bot.py:1803` saying the
brief goes "through the same `outbox.run_check`"; `atlas-02/research|
wc26-picks` over the 2026-09-09 ruling; `hdr|HDRI|photograph|D40` over
queue 186, 12 hits, the HDRI half at 86 to 103; `cleared out of the outbox|
sent messages|outbox archive|...` over the queue, nothing filed on the
structural point of section 3.

## 1. The answer in one paragraph

THE BATCH LANDS, with one statement of the research ladder replaced by hand
(section 8, finding 1), the dictated text of section 10 applied, the gate's
done line pasted into the commit message from the run and not from the
report, and no number that nobody printed. The forty-name waiver is the
right shape for its day and the right membership (every checked file that is
not on the legacy list, reconciled to the gate's 42 in section 2), and the
third frozen list does not cross a line; the line is drawn here at a fourth,
and the question under all three is filed with a number. The walk-back
stands as his number in the reading that makes his words literally true of
what a grade is; the 0.368 that made it look tuned toward the sheet is an
artefact of an exponent fitted in one space and applied in another, and is
withdrawn as a prediction with the arithmetic shown. The provenance guard
implements what is checkable from a workflow that commits nothing, which is
the amendment's intent and not its text, and the amendment is marked
superseded where it sits. The done line is three and the three are named,
with a clause for dated transcripts. The ruled URL stays with zero live
users because it is the only lawful way the research can be linked at all,
and its dead retirement item is queue 275's class. Two guards landed without
their rejecting fixtures and that is queue 306, cheap and owed.

## 2. What I verified, claim by claim, and where the brief to me was thin

THE FORTY, COUNTED. `PRE_MOVE_MESSAGES` (`producer-check.py` 346 to 388)
holds 40 names: 7 under `production/briefs/` (06, 07, 09, 10, 12, 14, 15)
and 33 under `production/outbox/`. The outbox listing has 34 message files
and a README; the one not on the list is `2026-09-05-the-console-run.
unprompted.md`, which is on `LEGACY_LINK_RULES` (272). The briefs listing has
12 files; the five not on the list are the four `PRE_REGISTER` names (3797
to 3802: 08-31, 09-02, `latest.md`, the step-1 report) and `2026-09-05.md`,
on `LEGACY_LINK_RULES` (273). So the walk is 47 files, 5 exempt by name,
42 checked, and the 42 are exactly 40 on this list plus the 2 legacy files,
which carry github.com links admitted under the retired host list and so
satisfy the floor with a link. That is `filesLinkFloorOff=40/42` as queue
259 quotes it, reconciled from the directories and not from the report.

THE RECEIPTS, MATCHED BY NAME. The comment at 333 to 336 says 37 of the 40
carry a send receipt and names the three that do not. Listed
`production/outbound/`: every one of the 33 outbox names has a
`<name>.receipt.txt` beside it, and `brief-2026-09-09`, `-10`, `-14` and
`-15` exist. 33 plus 4 is 37; the three without are briefs 06, 07 and 12.
The claim reproduces exactly. Today's brief was sent at 03:34:08Z as
messageId 99 with `buttons: 2/2` off `fileCommit e7564eb5` (its receipt),
and the page was measured served at 05:20:56Z (the marker), so the newest
name on the list was already on his phone before the floor came back.

THE DOOR NEVER LEARNS THE LIST, and the brief to me did not say so. `main()`
at 4489 to 4492 passes `research_verbatim` by name through `rel_under` and
does not pass `pre_move`, so the single-file path, which is what
`tools/runner/outbox.py:run_check` and the brief sender run (259 item 2;
`telegram-bot.py:1803`), grades a listed name under the floor in force at
the moment of sending. The outbox README (141 to 152) records the identical
asymmetry for `LEGACY_LINK_RULES`, measured on both files on 2026-09-06 and
ruled gate-only by design. Section 3 rules it the same way here and section
10 writes it down where the list is.

THE 0.368 REPRODUCES, AND ITS ARITHMETIC IS NOW NAMED. Queue 299 (308 to
316) says the tension comes from a one-point model on the kerb, rendered
0.522 against texel 0.406, and gives 0.368, 0.334 and 0.348 for the three
readings. All three reproduce from one formula, section 4, so I know which
model produced them and can say what it mixed.

THE G++ NUMBERS REPRODUCE. Ground, red: 0.74 x 0.55 = 0.407; walked back
1 - 0.85 x 0.593 = 0.49595; byte 126 (0.49595 x 255 + 0.5 = 126.97, floored
by ByteOf); linear ((0.49595 + 0.055)/1.055)^2.4: 0.52223, ln -0.64966, x2.4
-1.55918, exp 0.21031. Green 0.418 to 0.5053, byte 129, linear 0.2190; blue
0.44 to 0.524, byte 134, linear 0.2369. Wall, red 0.74 to 0.779, byte 199,
linear 0.56884; green 0.796, 203; blue 0.830, 212. The test pins
`grade-on-white.126.129.134`, `linear.0.2103.0.2190.0.2369` and
`199.203.212` (2536 to 2550, 2704 to 2711), and the wake's prediction for
run 45 carries the same bytes. The one call site at `SurfaceBind.h` 660 sits
after `GradeChainGamma` and before the single `SrgbToLinear`, in gamma,
which is the order the block at 577 to 583 rules.

THE 10/10 WAS NEVER WRITTEN ANYWHERE. Zero hits for the waiver bit at ten
over every markdown, text and python file, zero hits for
`researchVerbatimWaiverBit` outside the tool other than the 2026-09-14 `4/5`,
and nothing has been committed since `5a8ef789`, so no record, wake, brief
or verdict carries the wrong number. The builder's claim holds on the
population that could carry it. What the tree does carry is the fault's
description at 4080 to 4086, quoting 5 of 10 to 10 of 10, which is the right
place for it. Section 8 finding 1 is about the arithmetic that let it
happen.

THE 2026-09-09 RULING WAS APPENDED, NOT REWRITTEN. Section 7 (464 to 486,
dated 2026-09-15) carries the moved URL and the `git ls-tree` measurement of
the five files behind it; line 311 still carries the URL as ruled that day.
That is the right form for a LOG record and the selftest's "the ruling record
still carries the URL" (2828 onward) is satisfied by the appended section.

THE CORRECTION AT `VignetteShot.cpp` 175 TO 204 keeps reasons 1 and 2 as
work, names D40, prints the six sky numbers and says in as many words that
the atmosphere does not render blue, which is what D40 requires of it. D40
itself says at line 30 that reasons 1 and 2 "become queue 299's successor"
and at line 83 that queue 186 carries them; 186 does (86 to 103). One word
in an uncommitted record, corrected in section 10 before it lands.

THE MARKER. `production/site-served.txt` line 107 carries exactly one
`servedCommit=` line, a 40-hex sha equal to the reference commit,
`servedUrl` equal character for character to `SITE_ORIGIN` at
`producer-check.py:663`, and `printedBy` naming run 27, id 34932214186, job
104262618410 and step 13. The guard's four readings all hold on it by
inspection, and `merge-base --is-ancestor` of a commit against itself exits
0, so the reference commit being the served commit is the accepting case and
not an edge.

## 3. Call 1: the forty are waived by name; the shape and the membership are right; the line is a fourth list, drawn here

THE QUESTION. Shipping the two halves of queue 256 in one commit, as ruled
on 2026-09-10 and amended on 2026-09-11, re-grades every message in the two
walked trees. The builder's ladder (299 to 312 of the tool, one run, one
contributor toggled per rung) puts the fault where it is: 19 of 42 red on
the origin alone, 20 on the floor alone, 39 on both, 40 once `RULED_LINKS`
moved. `ledger/verify.py` walks that register before every commit, so
without a waiver the whole project holds red on messages Jafar has already
read.

THE ROUTES.
- A. Edit the forty to carry the live link or a link at all. REFUSED. They
  are the record of what he was sent (outbox README 20 to 25, "a sent
  message stays here"), and rule 5 says copy what a human spent time on, not
  rewrite it. A message he read on the 9th that now carries a link written
  on the 15th is a false record with a green gate under it.
- B. Move them out of the walked trees. REFUSED. The README rules a
  subdirectory of the outbox is walked too, on purpose; the receipts and the
  two other lists name these paths; and 40 renames is a batch of its own.
- C. Select the rulebook by the date in the filename. REFUSED, on the
  reasoning already written at `LEGACY_LINK_RULES` and repeated at 327 to
  330: the specimen would choose its rulebook by naming itself. Queue 077
  used the filename date for the clock and said why that is different (a
  deadline is a fact about the message; a rulebook is a fact about the
  world).
- D. Turn the floor off again or widen the band until the forty pass.
  REFUSED. Rule 2 and this director's standing refusal: no threshold moves
  to make red go away, and no plan begins by weakening an instrument.
- E. A frozen tuple of names waiving exactly the two rules the move broke,
  counted with denominators, with a rot check, gate-only. TAKEN. It is the
  third instance of one idea (LEGACY_LINK_RULES 2026-09-06, RESEARCH_VERBATIM
  2026-09-14) and not a new one; every rule not named still binds
  (`linkcap` is tested as the third rule at 3625 to 3640); the accepting
  case comes first at 3589 to 3604, the rejecting case at 3608 to 3620 is
  the same tree with the list empty, and the rot case at 3644 to 3654 is a
  name no file answers.

RULED: E, as built. THE SHAPE IS RIGHT because the fault is not in any
message: each of the forty was correct under the rulebook in force when it
went, and the ruling of 2026-09-10 made zero links LEGAL for the whole
`servedCommit=none` window, so a gate that fails them today is today's
rulebook grading yesterday's specimen. THE MEMBERSHIP IS RIGHT because it is
exactly the checked population minus the legacy pair (section 2), grounded
on when each was written and not on whether a receipt was found; the three
without receipts are named in the tool and here. A message written from now
on is on no list and faces the full floor, which is the property the floor
exists for. THE THIRD LIST DOES NOT CROSS A LINE, and the line is written
here so the next reader has it: a FOURTH frozen name list does not enter
`tools/producer-check.py` without a ruling that first answers queue 307,
because three lists in ten days is the measurement that the lists are
patches on one structural fact, that the walked trees are an archive by rule
and the gate re-grades the archive under whatever rulebook is current. The
next rung is named, not taken: whether a sent file's rulebook generation
should be selected by its receipt, a fact the sender writes and the specimen
cannot choose, so the three lists collapse to one rule plus the receipt-less
names. That is design work with a real trade-off (a receipt is a second
tree; a forged receipt is a false sentence of the same class as a false
stamp) and it waits behind game work under the spend rule.

QUEUE: 307 three-frozen-name-lists-grade-an-archive-and-the-fourth-is-the-line

TWO CONDITIONS, BOTH TEXT.
1. The gate-only asymmetry is written where the list is (section 10, item
   2): at the door a listed name faces the live floor, which is the right
   direction, and the three receipt-less briefs, if ever sent, are refused
   on `linkfloor` there and rewritten with a link or dropped. Nobody "fixes"
   the door by passing `pre_move` to it.
2. Queue 303 learns that the 12th cannot be its accepting specimen as
   written (section 10, item 6), because the specimen it names is linkless
   and the floor is live at the door since 05:20:56Z.

## 4. Call 2: the reading stands as his number; the 0.368 is withdrawn as a prediction; nothing goes back to him before the frame

THE READING, ON HIS WORDS AND NOT ON A NUMBER. He said the grade OVERSHOOTS
and the frame is DARKER than his sheet, and to take it to "about 0.85 of
what landed". A grade multiplies base colour, so every reading that
multiplies the grade by 0.85 moves the frame darker, into the overshoot he
asked to undo; the resident's three candidates were all multiplies and the
builder was right to refuse all three on his own sentence. Of the readings
that move the right way, only one makes "0.85 of what landed" literally true
of the thing a grade is: a grade's whole content is its distance below
white, and G' = 1 - 0.85 x (1 - G) keeps 0.85 of the darkening that landed.
G/0.85 and G^0.85 are not 0.85 of anything he named. It composes in gamma
where the other two terms compose (`SurfaceBind.h` 387 to 394), it anchors
(strength 1.0 is run 44 exactly, 0.0 is white), it is a separate term with
his date on it, and it prints on every graded line as
`/jafarWalkBack.0.85..ruled.2026-09-15` so no frame can be read against it
without learning it is provisional. RULED: it stands, as his number, in that
family.

THE TENSION, AND WHY IT DOES NOT SURVIVE. Queue 299 says this reading
predicts a ground p50 near 0.368 against his 0.373, closer to the sheet he
said not to tune toward than G^0.85 (0.334) or G/0.85 (0.348). All three
figures reproduce from one formula, so this is the model and not a guess at
it:

    p50 = 0.2711 x (Lin(G') / Lin(0.407))^k,  k = ln 0.522 / ln 0.406 = 0.7213

    Lin(0.4960) = 0.2104   ratio 1.526   1.526^0.7213 = 1.357   0.368
    Lin(0.4658) = 0.1837   ratio 1.333   1.333^0.7213 = 1.230   0.334
    Lin(0.4788) = 0.1950   ratio 1.415   1.415^0.7213 = 1.284   0.348

The exponent k was fitted on two GAMMA-BYTE ratios, the kerb's rendered
93.7/179.7 = 0.522 against its texel 74.8/184.3 = 0.406 (queue 299 lines
216 and 226), and then applied to a ratio of LINEAR grades. That is one
number in two spaces. The same one-point model applied in the space it was
fitted in, gamma to gamma:

    0.4960/0.407 = 1.219   1.219^0.7213 = 1.153   0.313
    0.4658/0.407 = 1.145   1.145^0.7213 = 1.102   0.299
    0.4788/0.407 = 1.176   1.176^0.7213 = 1.124   0.305

and fitted and applied in linear (k = ln 0.522 / ln 0.1378 = 0.328):

    1.526^0.328 = 1.149   0.311
    1.333^0.328 = 1.099   0.298
    1.415^0.328 = 1.121   0.304

Under either consistent form the three readings sit at 0.30 to 0.31, short
of the sheet by a similar margin, and differ from each other by less than
anything a one-point model can resolve. The near-coincidence with 0.373 was
a property of the mixed arithmetic and not of the reading. The builder had
already flagged the model as soft and rule 2's own failure, designed nothing
from it, and drew its direction conclusion model-free; that was right. What
was wrong was letting the number stand in an item and in tomorrow's brief
plan as a prediction.

RULED.
1. The 0.368 is WITHDRAWN as a prediction. No predicted p50 enters queue
   299, the DISPATCH file or the brief. Run 45 prints the number.
2. No member of the family is preferred by its predicted distance from
   0.373 in either direction. Choosing the reading that lands nearest the
   sheet would be tuning toward it; choosing the one that lands furthest
   would be tuning away from it, the same instrument error mirrored. The
   constant was fixed before the run and is his.
3. It does NOT go back to him as a question before the frame. D23 gives him
   the frame, he answered with a direction and a number, and the reading is
   the one his words support. The brief carries ONE sentence: the reading
   taken (0.85 of the darkening kept, in gamma), and that G/0.85 and G^0.85
   are named in the header with their numbers if his eye says otherwise.
4. If run 45's p50 lands near 0.373 it is reported as a measured
   coincidence under his own label, the sheet is wet and the street is dry,
   and never as a tuning. If it lands at 0.31 that is what a one-point
   model would have said; both are readings and neither is a bound.
5. The residual the builder reported stands as reported: the ten procedural
   pieces stay at the full legacy grade because `ProceduralAlbedoTexel`
   reproduces a Unity value and an engine-local constant inside a parity
   value is a parity value that has silently stopped being one. The
   resident's diagnosis called it a double-grade; the builder's correction
   is the right reason and is the one kept (`SurfaceBind.h` 441 to 473).

## 5. Call 3: the provenance guard implements what a workflow that commits nothing can be checked against, and the amendment's text is marked superseded where it sits

THE FACT. `.github/workflows/publish-glance.yml` runs under
`permissions: contents: read` (152 to 155), has no `git add`, `git commit`
or `git push` in any of its steps (read whole), and its verdict goes to
`$GITHUB_STEP_SUMMARY` and nowhere else (418 to 439). There is no CI commit
carrying a verdict file, so the 2026-09-11 amendment, "printedBy names the
short sha of the CI commit carrying the verdict file, and verify checks a
file in it carries the same sha", describes a guard that can never pass.
Implemented literally it would hold every commit red for a reason nobody
could act on, which is the validator-nothing-survives shape this project has
paid for.

WHAT WAS BUILT INSTEAD (`verify.py` 1785 to 1877), all offline: a whole
40-hex sha, not a prefix; an object that exists here and is a commit; an
ANCESTOR of HEAD rather than HEAD, because publish-glance rides every push
and the served commit is routinely behind by the time anything reads the
marker; `servedUrl` equal to the string `producer-check.py` sets
`SITE_ORIGIN` to; `printedBy` naming publish-glance and a run id. Red, not a
warning, folded into `producer_register()` at 1910 so it reports on the same
line as the walk it governs; called, so rule 6 is met.

WHAT IT IS STRONGER AT AND WEAKER AT, in the builder's own words, which are
right: it points at the run, job and step that printed rather than at a
commit that might carry a file; it re-reads nothing, so git refutes an
invented sha or a foreign one but not a plausible ancestor sha typed beside
a made-up run id. The run id is what a human re-queries. That boundary is
written in the docstring (1825 to 1835) and it is the honest one for a gate
that runs before every commit in a container whose network is not
guaranteed.

RULED: the substitution is ACCEPTED as the amendment's intent, which was a
printed fact beating a typed one. The amendment's text is not rewritten in
the 2026-09-11 record, which is a LOG; it is marked superseded in queue 256
beside the amendment (section 10, item 4a), with the pointer to the Step 2
section that already explains why. NEXT RUNG, named and not taken: the one
place the run CAN be re-read is a runner with network, and the publish job
already checks out the tree, so a print of whether the marker's
`servedCommit` is an ancestor of the sha it is publishing would cost one
line of an existing step and no instrument; it is a print, not a gate, and
it goes with 307's answer rather than as its own item. WHAT IS OWED NOW: the
guard's rejecting cases have never run (section 8, finding 3), queue 306.

## 6. Call 4: the done line is three, the three are named, and the carve-out gains a clause for dated transcripts

`grep github.io/wc26-picks tools/` reads 3: `producer-check.py:671`, the
`ARCHIVE_ORIGIN` constant that the rejecting fixture needs and amendment A2
already carves out; `map-notify.py:40`, the recorded output of a curl run
made on 2026-09-06 against the origin that was live that day (`curl: (56)
CONNECT tunnel failed, response 403 http=000`); and `map-notify.py:44`, the
sentence saying line 40 is a transcript and must not move. The file is
RETIRED (its banner, lines 1 to 20: nothing calls it and it refuses to run),
and its nine fixture links, which are links a message would carry, DID move
to `github.io/ledger/` (613 to 783 by grep).

RULED: the done line is SATISFIED, and not merely in spirit: rewriting a
recorded measurement to name an origin nobody measured against would claim a
measurement nobody took, which is rule 1's fault in a comment. The carve-out
gains its clause in writing (section 10, item 4b): a dated transcript of a
measurement stays, provided the same lines say it is a transcript and name
the date, and the count on the done line is stated as three with the three
named. A fourth hit is a finding and not a fourth clause. No instrument is
added to count them; the grep is the instrument and the number is in the
item.

## 7. Call 5: the ruled URL stays with zero live users, and its dead retirement item is queue 275's class

THE FACT. `RULED_LINKS` (851 to 855) holds one entry, moved by this batch to
the `ledger` tree URL as the 2026-09-10 ruling ordered. The one message that
ever used it, the atlas-02 digest of 2026-09-09, carries the old URL (its
line 4, the `wc26-picks` tree at the dead branch) and is on
`PRE_MOVE_MESSAGES`, so it passes on `linkdest` waived and matches no ruled
entry. The live walk therefore reads `linksRuledUsed=0/1`, which is the
honest reading and the one the selftest deliberately does not bound (3091
to 3105). The item that retires the entry, queue 184 (publish the research
as a page and delete the entry in the same commit), is CLOSED 2026-09-10 by
the archiving commit `cd55a79c` with, in its own words, no ruling naming the
closure, reviewed 2026-09-14 and not reopened. So the exception's retirement
path is closed while the exception lives.

THE ROUTES.
- Delete the entry now. REFUSED for tonight. An empty tuple makes
  `linksRuledUsed=0/0`, which needs the zero-denominator words the research
  bit already prints, and moves the RULED_DIGEST fixtures at 3063 to 3139:
  instrument work on a batch that is already 1002 lines, and it would also
  delete the only lawful way the research can be linked to him at all.
- Keep it. TAKEN. Jafar asked for the digest "with the link" and the link on
  his phone now points at a branch that does not exist on the repository the
  project lives in. Whether he is told, and whether a corrected digest is
  sent, is the Producer's call in the next brief and not this ruling's; if
  one is sent, this entry is exactly what admits it, and the reading moves
  to 1/1 by itself.

RULED: the entry stays; `0/1` is a reading, not a fault; its retirement item
being closed under `cd55a79c` without a ruling is one of the hundred and
eighty-two queue 275 already names, and this record adds 184 to that item's
evidence by name rather than reopening it under a ladder that stages.md does
not put it on. The exception may not widen, and the day a page carries the
research it dies in the same commit, exactly as 184 says.

## 8. Call 6: what is wrong in the diff, ranked, and which of it blocks

1. THE RESEARCH LADDER STILL COUNTS BY DIFFERENCE. `producer-check.py` 4087
   to 4091: with `pre_move` now carried, the second rung toggles one
   contributor, which is right, but `waiver_removed` is still
   `len(plain) - len(res)`, a count of ANY findings that moved, where the
   pre-move rung beside it (4103 to 4104) counts only findings whose rule is
   on the set it actually waived. The fault the builder found and fixed was
   exactly a second contributor leaking into a raw difference, and the next
   list added to this file re-opens it. ONE STATEMENT, dictated in section
   10 item 1: count `plain`'s findings whose rule is in
   `res["research_waived"]`. On the live tree this is a no-op by
   construction (with one contributor toggled the only findings that can
   move are `banned`), which is the guard: the resident prints
   `researchVerbatimWaiverBit` before and after and the two must be equal;
   if they are not, the fix is reverted and the disagreement is the finding.
   A CONDITION OF LANDING.

2. THE 0.368 IN TWO PLACES. Queue 299 line 309 as "predicts", and the 05:51Z
   wake line 9 as the tension the brief is to carry. Section 4 withdraws it;
   section 10 items 3 and 9 carry the text. A CONDITION OF LANDING on the
   text and on the brief.

3. THE PROVENANCE GUARD HAS NO REJECTING FIXTURE. Two hits for its name in
   the file, none a fixture, no `markerProvenance=` refusal asserted
   anywhere. I read every refusal branch (1846 to 1875) and each returns
   red, so an untested branch here fails loud rather than silent; a guard
   read is not a guard run. Queue 306. Not blocking.

4. NO FIXTURE HAS A FILE ON BOTH LISTS. The ten research parts are on
   `RESEARCH_VERBATIM` and on `PRE_MOVE_MESSAGES`, which is how the 10/10
   happened on the live tree, and the suite's research case (2960) and
   pre-move case (3589) each plant files on one list only, so undoing either
   repair goes red nowhere. Queue 306, same item. Not blocking.

QUEUE: 306 two-guards-of-the-15th-landed-without-their-rejecting-fixture

5. THE DOOR ASYMMETRY IS UNDOCUMENTED FOR THIS LIST. Section 3, condition 1.
   Text.

6. QUEUE 303'S SPECIMEN. Section 3, condition 2. Text.

7. D40 LINE 30 says reasons 1 and 2 "become queue 299's successor"; they
   are queue 186's, as its own line 83 says. One phrase in an uncommitted
   record. Text.

8. THE RULED URL'S RETIREMENT ITEM IS CLOSED. Section 7. Not blocking.

9. `SurfaceBind.h` 1403 still prints `/groundGrade.not-a-ground-surface` for
   an untextured ground surface; ruled at 00:52Z to change with 293's touch.
   Unchanged tonight and not a finding twice.

10. THE 449. The resident's own g++ run, 447 to 449, 0 failed, and I did not
    recompile; the two additions I can see are the walk-back string at 2716
    and the re-pinned constants at 2536 to 2550 and 2704 to 2711, with the
    hand arithmetic in the comments reproducing to the digit (section 2).
    The direction is pinned by the constants themselves: a multiply reading
    would put 0.629 where 0.779 is asserted at 1e-12.

Nothing in the diff loosens a bound, moves a threshold, or weakens an
instrument. The floor is back at the ruled 1..2, the band is unchanged,
`linkcap` binds on the forty, the two legacy files still carry their links,
and the marker's failure direction is still loud.

## 9. Rule 5b: accepting and rejecting cases, and which run proves each

- The pre-move waiver: accepting, two listed files with a served marker pass
  (3589 to 3604); rejecting, the same tree with the list empty fails both
  (3608 to 3620); narrowness, a listed file over the cap is refused by
  `linkcap` alone (3625 to 3640); rot, a name no file answers is counted
  (3644 to 3654). The resident's selftest, 163 passed, 0 failed.
- The archive origin: the first day the case can fire, both moments on one
  path, `map.html` under the live origin admitted and under the archive
  refused (3661 to 3671).
- A1 on the live tree: rung 1 the live marker against the live origin,
  rung 2 a synthetic sha against the archive refused, rung 3 the same tree
  against the live origin accepted (3676 to 3706).
- The walk-back: the constants at 1e-12 and 1e-7 in both branches (2536 to
  2550, 2562 onward), the printed strings pinned whole (2704 to 2719), the
  never-reached line still `not-built` on all four (2731 to 2740). 449, 0
  failed, the resident's compile.
- The provenance guard: ACCEPTING ONLY, the live tree, in the resident's
  verify run. Rejecting: NOT RUN, queue 306.
- The research ladder's repair: no fixture with a file on both lists. NOT
  RUN in the suite; the live number is the only witness, and section 10
  item 1 asks for it before and after. Queue 306.
- The frame: NOT PROVEN BY ANYTHING ABOVE. Run 45 is the proof and section
  11 is how it is read.

## 10. Dictated text

Every replacement verbatim; where a sentence is replaced, grep the sentence
and replace every copy (rule 1); counts into the commit message. No file
outside this list is edited under this ruling.

1. `tools/producer-check.py` 4090 to 4091, the two physical lines

       waiver_removed = max(0, len(plain["findings"])
                            - len(res["findings"]))

   become the one statement

       waiver_removed = sum(1 for f in plain["findings"]
                            if f.rule in res["research_waived"])

   with the indentation of the lines they replace. The comment above them
   (4080 to 4086) stays and gains, as its last sentence: `AND THE COUNT IS
   BY RULE NAME, as the pre-move rung below counts, so a third contributor
   cannot leak into a raw difference again (ruled 2026-09-15 05:43Z).` Then
   `python3 tools/producer-check.py --selftest` (163 expected, 0 failed) and
   `--gate`, and `researchVerbatimWaiverBit=` off the gate's done line
   BEFORE and AFTER, both pasted into the commit message. Equal, or the line
   is reverted and the inequality is reported as a finding.

2. `tools/producer-check.py`, inserted after the paragraph ending "so that
   never happens in silence." (line 345) and before `PRE_MOVE_MESSAGES = (`:

       # GATE-ONLY, EXACTLY AS LEGACY_LINK_RULES IS, AND ON PURPOSE (ruled
       # 2026-09-15 05:43Z). main() below, the single-file path that
       # tools/runner/outbox.py:run_check and the brief sender shell out to,
       # passes research_verbatim by name and does not pass pre_move, so at
       # the DOOR a name on this list faces the floor in force at the moment
       # of sending. That is the right direction: this list grades messages
       # that were already sent, and a message sent after the move carries
       # a link or does not go. Three names here have no receipt (briefs
       # 2026-09-06, -07 and -12); if one of them is ever sent it is refused
       # on linkfloor at the door and the Producer rewrites it with a link
       # or drops it. Do not "fix" the door by passing pre_move to it.

3. Queue 299, appended to its status:

       RULED 2026-09-15 05:43Z (decision-2026-09-15-ruling-the-forty-are-
       waived-by-name-and-the-walk-back-stands-as-his-number.md, section 4):
       THE READING STANDS AS HIS NUMBER, and THE 0.368 ABOVE IS WITHDRAWN AS
       A PREDICTION. The three figures in the tension paragraph reproduce
       exactly from one model, p50 = 0.2711 x (Lin(G')/Lin(0.407))^k with
       k = ln 0.522 / ln 0.406 = 0.7213: Lin(0.4960) = 0.2104 gives
       1.526^0.7213 = 1.357 and 0.368; Lin(0.4658) = 0.1837 gives 0.334;
       Lin(0.4788) = 0.1950 gives 0.348. The exponent was fitted on two
       GAMMA-BYTE ratios (rendered 93.7/179.7, texel 74.8/184.3) and applied
       to a LINEAR ratio. Applied in the space it was fitted in, the same
       one-point model reads 0.31 for the reading taken (1.219^0.7213 =
       1.153), 0.30 for G^0.85 and 0.31 for G/0.85; fitted and applied in
       linear (k = ln 0.522 / ln 0.1378 = 0.328) it reads 0.31, 0.30 and
       0.30. The near-coincidence with the sheet's 0.373 was a property of
       the mixed arithmetic and not of the reading, and under a consistent
       model the three readings differ by less than the model can resolve.
       NO PREDICTED p50 ENTERS THIS ITEM OR THE DISPATCH: run 45 prints it.
       The reading is kept because it is the one under which "0.85 of what
       landed" is literally true of what a grade is, its distance below
       white, and not because of any number it predicts. If run 45 lands
       near 0.373 that is a measured coincidence reported under his own
       label (the sheet is wet, the street is dry), never a tuning: the
       constant was fixed before the run and is his. Nothing goes back to
       him as a question before the frame; the brief names the reading
       taken and the two beside it in one sentence.

4. Queue 256, three insertions.
   (a) After amendment item 1 (the paragraph ending "watched it printed."),
       one paragraph:

       SUPERSEDED 2026-09-15 (ruling of 05:43Z, section 5): the workflow
       commits nothing, so there is no CI commit to name; the check is
       ancestry and origin, as "The provenance amendment described a
       workflow that does not exist" under Step 2 records. The intent, a
       printed fact beating a typed one, is unchanged.

   (b) Appended to "Done looks like":

       Ruled 2026-09-15 05:43Z: the count is THREE and the three are named
       (producer-check.py:671, ARCHIVE_ORIGIN; map-notify.py:40 and :44, a
       dated transcript of a measurement taken against the archive on
       2026-09-06 and the sentence saying so). The carve-out gains the
       clause: a dated transcript of a measurement stays, provided the same
       lines say it is a transcript and name the date. A fourth hit is a
       finding, not a fourth clause.

   (c) Appended at the end of the file:

       RULED 2026-09-15 05:43Z (decision-2026-09-15-ruling-the-forty-are-
       waived-by-name-and-the-walk-back-stands-as-his-number.md): step 2
       lands as built; PRE_MOVE_MESSAGES is the right shape and the right
       forty, gate-only as LEGACY_LINK_RULES is, and a fourth frozen list
       needs a ruling that first answers queue 307; the provenance guard's
       rejecting fixtures are queue 306.

5. Queue 259, appended to its last section:

       RULED 2026-09-15 05:43Z: the floor is back at the ruled 1..2 and the
       forty are waived by name at the gate only; at the door every name
       faces the live floor. Item 4 unchanged, still waiting on 260.

6. Queue 303, appended to its status:

       RULED 2026-09-15 05:43Z: the 12th's brief as written cannot be this
       item's accepting specimen. It is linkless, the floor is live since
       05:20:56Z, and the single-file check the send step runs does not
       apply PRE_MOVE_MESSAGES (gate-only by design, producer-check.py at
       the list), so a dispatch naming 2026-09-12 today produces a refused-
       record on linkfloor and no receipt. The accepting specimen is a brief
       carrying a link, or the 12th rewritten with one, and whether the 12th
       is rewritten at all is the Producer's call.

7. `ledger-v2/respec/decision-register/D40-the-sky-is-a-photograph-not-an-
   atmosphere.md` line 30: "They become queue 299's successor and the
   builder solves them or reports precisely why it cannot." becomes "They
   are queue 186's, and the builder solves them there or reports precisely
   why it cannot." Uncommitted record, corrected before it lands.

8. `ledger-v2/respec/decision-register/rulings-log.md`, one entry after the
   2026-09-15 Jafar entry, in the file's form:

       - **2026-09-15** the forty are waived by name and the fourth list is
         the line: PRE_MOVE_MESSAGES lands as the right shape and the right
         forty, gate-only, the receipt question is 307; the 0.85 walk-back
         stands as his number as a strength from white in gamma and the
         0.368 is withdrawn as a prediction (an exponent fitted in gamma and
         applied in linear); the provenance guard checks ancestry and origin
         because the workflow commits nothing, its rejecting fixtures are
         306; the done line is three, named, with a clause for dated
         transcripts; the ruled URL stays with zero users and 184's closure
         is 275's class
         `game-design/decision-2026-09-15-ruling-the-forty-are-waived-by-name-and-the-walk-back-stands-as-his-number.md`

9. `production/NOW.md`, a new dated block at the top of the log, in the
   file's form, saying: the batch of 299 (walk-back), 256 and 259 (links,
   marker, floor) landed under this ruling; run 45 is the next dispatch and
   the wake at 05:51Z is its plan, with ONE change to that plan: the brief
   carries no predicted p50 and does not carry 0.368; it carries the reading
   taken and the two alternatives in one sentence, and run 45's measured
   p50 beside 0.2711, 0.5117 and 0.373 with his label. Queues 306 and 307
   filed by this ruling.

10. `production/quality-ladder.md` line 134, the last cell's final clause
    "then the number itself, judged by Jafar against the Hook sheet under
    D23, recorded as his." becomes "then his provisional 0.85 walk-back
    (ruled 2026-09-15, a strength from white in gamma, one site) re-read on
    run 45's frame, and re-read again when wetness lands (186) because he
    said it is provisional and not kept."

11. `production/wakes/2026-09-15T0551Z-29c6df09.wake.txt` is the resident's
    and is not edited; its brief plan is overridden by item 9 on the one
    point of the 0.368.

## 11. The landing read for run 45, so the next director reads the run and not the colour

The sha captured before dispatch, the run watched by ancestry. In this
order:

(a) THE FRAME FIRST, `ue-vign_hook_day.png` beside run 44's and beside
    `ce99814`'s, before any number: the kerb, the road, the trim, the
    railings and bins, the plaster, one sentence each; and the ten
    procedural pieces (interior, paint_yellow) beside their neighbours,
    because they are the residual the builder named.
(b) THE TWELVE LINES: every RESOLVED pack line carries
    `/jafarWalkBack.0.85..ruled.2026-09-15`; the four ground lines
    `tintTexel=grade-on-white.126.129.134` with
    `/linear.0.2103.0.2190.0.2369`; the eight others `199.203.212`; the two
    procedural lines unchanged at `31.22.14` and `147.127.35`. Twelve of
    twelve, counted.
(c) THE GROUND: `band.ground.p50` beside run 44's 0.2711, `ce99814`'s
    0.5117 and the sheet's 0.373, direction UP from 0.2711, residual to each
    stated, NO BOUND and NO PREDICTION QUOTED. `p05` beside 0.1217,
    `meanLuma` beside 0.2490, `meanRGB` beside 62.2/63.4/67.4 with blue
    still highest.
(d) THE CONTROL: `band.skyCentre.p50` at 0.8035 to four places and
    `shotExposurePinRead` 0.3000/0.3000; if either moved, suspect exposure
    or instrument before the grade (rule 3). `skyHdriFoundAt` still
    NOT-FOUND, because this batch binds no sky.
(e) TOO DARK AS A NUMBER: `shotMinLuma` beside 0.0659, `shotClipLoAll`
    beside 0/921600, the darkest luma band beside 31431 of 921600; the
    direction should be back toward run `ce99814`'s 2440 and the size is
    the reading.
(f) `materialStatus` beside run 44's COMPILE-UNPROVEN, reported as is.
(g) NO-READ escalated per C4 rather than quoted around.

Then the frame to Jafar, the Producer's channel, one message, with section
4's sentence and nothing asked.

## 12. The quality ladder at close

The grade: first working on the walk-back and named as such; the rung being
taken is run 45's frame, then 302 (the trim sentence, which cam_hook cannot
give), 293 (tally and readback), 186 (wetness and the photograph as the
sky, where his 0.85 is re-read), 300 (the flat cards), then his number
again. None blank.

The channel: the links point at a page a run measured served; the floor is
live; the archive is graded under a name list for the third time. The next
rung is 307, a receipt-keyed generation or a written reason there is none,
and it waits behind game work by the spend rule.

The guards: two landed with their accepting case only; 306 is the rung, and
it is cheap.

## 13. For the commit message

Queue 299 applies Jafar's provisional 0.85 as a strength from white in
gamma at one site (`AlbedoGradeFor`), leaves `ProceduralAlbedoTexel` a
parity value, prints the strength and its date inside `tintFrom` on every
graded line, and re-pins the g++ constants (449/449, 0 failed, the compile
line pasted). D40 is recorded and the `VignetteShot.cpp` position corrected
without claiming the atmosphere renders blue. Queue 256 step 2 moves seven
sites and two tree links to `jsab258.github.io/ledger/`, writes the served
commit `5a8ef789` printed by publish-glance run 27 into the marker, brings
the floor back to 1..2 (259), waives the forty messages already sent by name
for exactly `linkfloor` and `linkdest`, and adds a provenance guard that
checks ancestry and origin because the workflow commits nothing. Ruled
2026-09-15 05:43Z (decision-2026-09-15-ruling-the-forty-are-waived-by-name-
and-the-walk-back-stands-as-his-number.md): the forty are the right shape
and the right membership, the fourth list is the line (307); the walk-back
stands and the 0.368 is withdrawn as a prediction; the guard's substitution
is accepted and the amendment marked superseded; the done line is three,
named; the ruled URL stays at 0/1; two guards owe their rejecting fixtures
(306). One statement of the research ladder replaced by hand; the gate's
done line pasted here from the run: `researchVerbatimWaiverBit=` before and
after, `filesLinkFloorOff=40/42`, `linksRuledUsed=0/1`,
`markerOriginConsistent=true`, and the `markerProvenance=` token; selftests
163 and the g++ 449; `cards.py --selftest` 71/1 pre-existing and filed as
305.

## 14. Refused

- Editing, moving or re-linking any of the forty sent messages.
- Selecting a rulebook by the date in a filename.
- Turning the floor off again, widening the band, or any bound moved to
  make the forty pass.
- A fourth frozen name list without a ruling that first answers 307.
- Choosing a member of the walk-back family by its predicted distance from
  the sheet, in either direction.
- Any predicted p50 in an item, a dispatch or a brief.
- Sending the reading back to Jafar as a question before run 45's frame.
- Implementing the 2026-09-11 amendment literally against a workflow that
  commits nothing.
- Rewriting the transcript at `map-notify.py:40`.
- Deleting the ruled URL tonight, or reopening 184 under a ladder stages.md
  does not put it on.
- Any sentence to Jafar that calls 0.85 settled, derived, or tuned.

## 15. CORRECTION, same session: item 1 of section 10 was wrong and is replaced

The resident applied item 1 and measured, both ways on the live tree: before
(raw difference) researchVerbatimWaiverBit=5/10, selftest 163 passed 0 failed;
after (the dictated statement) 0/10, 162 ok, 10 FAILED, one of them "the walk
counts what it waived over both denominators (1/1 graded, bit 0/1)". Reverted
and reported, which is the branch the condition named.

THE MECHANISM: producer-check.py builds a banned finding as
Finding("banned:" + label, ...), so its rule is `banned:run internals` while
res["research_waived"] holds the bare name `banned` (RESEARCH_WAIVED_RULES is
("banned",)); the statement compared the full string against the name and
matched nothing. The pre-move rung is unaffected because linkfloor and
linkdest findings carry bare names.

READING (b), that 5/10 was itself an artefact, IS EXCLUDED: with one
contributor toggled `plain` is a superset of `res` and the raw difference is
the count of banned findings, the same set a by-name count yields, and 5/10
was measured on 2026-09-14 before a second list existed.

THIS DIRECTOR'S FAULT UNDER RULE 1: the shape of Finding.rule was asserted
from the pre-move rung without reading the Finding() that builds it.

THE CORRECTED STATEMENT counts on the prefix before the colon, the suite's own
idiom: waiver_removed = sum(1 for f in plain["findings"] if
f.rule.split(":", 1)[0] in res["research_waived"]). ONE ATTEMPT, and it was
taken: 5/10 before and after, selftest 163 passed 0 failed, so it lands.
Finding 1 of section 8 stands as a finding and its size is unchanged: a third
contributor could leak into a raw difference, and today none does.

<!--RULING spawn=2026-09-15T05:43:41Z-->
