# Ruling, 2026-09-15 (22:25Z): per-condition wetness lands, the impossible ask is replaced by the per-shot key, the rename is upheld, the route filter protects ten instances from a write of their own defaults, 313's card is very probably the instrument, and the licence record owes the shipped pack its row

STATUS: LOG, 2026-09-15. NOT CURRENT once the first landed run whose commit
CONTAINS this batch's commit has been read in the order of section 10; from
then the verdict file, the g++ suite, queue 309, 322, 323 and 324 are the
reading copies and this is the record of what was ruled and why.

Director ruling on the two-builder batch of 2026-09-15 evening: half A, queue
309 (per-condition wetness, `ue-probe/Source/LedgerProbe/Public/SurfaceBind.h`,
`Public/VignetteSpec.h`, `Private/VignetteShot.cpp`, `tests/vignette-spec-
test.cpp`, GATED), and half B, the citypack attribution fix (`THIRD-PARTY.md`,
`tools/attribution-check.py`, `tools/citypack/fetch_textures.py`,
`.github/workflows/citypack-shortlist.yml`, four proof PNGs deleted, UNGATED
under D45 and read here only where it touches the licence record). Escalated
because `director_cadence` counts 1265 gated lines of 2446 changed against the
100 bound (the resident's figure; NOW.md's 22:10Z block read 445 of 1599 while
the builders were still typing, and neither number is re-derived here).

Author: tier-1 director, stamp at the foot naming row 652 of
`.claude/agent-log.tsv` (`2026-09-15T22:25:05Z` TAB `studio-director` TAB
`fable` TAB `default` TAB `a5c2cc1041900065b`). Row 638 (19:43:24Z) is the D38
director and predates the reference commit; rows 647, 648 and 651
(`engine-specialist`, `a795a0601b5bd0f53`) and 642 to 646, 649 and 650
(`content-wrangler`) are the two builders' lives and are not claimed. THE
REFERENCE COMMIT AS STATED TO ME IS `24b54723` at 20:40:00Z; `.git/logs/HEAD`
lines 103 and 104 show HEAD has since moved to `31f62ac0` at 21:08:22Z ("The
dashboard catches up with D38"). Whichever of the two the gate reads, row 652 is
newer than both. No code was written by this director, this director has no
shell, and no `git diff` was available: the tree was READ, and the builder's
report was not.

## 0. What was opened

`VignetteShot.cpp` 500 to 700 (the globals: `GByName` 510, `GBinds` 613,
`GWetness` 639, `GWetRedrive` 645), 1546 to 1560 (`FindCamera`,
`FindCondition`), 1570 to 1879 (`ReDriveWetness` whole and `ApplyCondition`
whole, the call at 1878), 2309 and 2507 to 2528 (`WriteVerdict`: shot lines,
surface lines at 2514, the materials line with `WetRedriveSegment` at 2528),
2869 to 2881, 3020 to 3079 (the per-shot group at 3039 to 3061), 3707 to 4174
(`BindSurfaces` whole: the piece loop at 3927 to 3950, the decal-card route
4031 to 4071, the pack and tint instance 4074 to 4146), 4249 to 4292
(`MakeControlTexture`, `ControlCamera`), 4297 to 4392 (`SpawnControlQuads`),
4455 to 4545 (the settle phase, `ApplyCondition(*Cond)` at 4535), 4676 to 4701
(the interactive call). `SurfaceBind.h` 429 to 433, 600 to 720, 860 to 970,
1225 to 1269, 1455 to 1660, 1700 to 2170, 2488 to 2502, 2823 to 2841.
`VignetteSpec.h` 2555 to 2654 and 2740 to 2799. The test 3280 to 3810 and 4915
to 5024. `production/specs/vignette-pieces.json` 20 to 106. Run 46's verdict
`production/d1-probe/ue-vignette-verdict.txt` by key over lines 186, 189 to
196, 232 to 248, 273 and 325. `production/d1-probe/DISPATCH` 1465 to 1514.
`THIRD-PARTY.md` whole (331 lines). `tools/attribution-check.py` 40 to 280 and
480 to 839. `tools/citypack/fetch_textures.py` 585 to 644 and the four hits
for `_write_shortlist_attribution`. The shortlist workflow whole (164 lines).
`tools/citypack/shortlist-2026-09-15-network-blocked-proof/` by glob (NOTE.txt
alone remains). `ledger/Assets/StreamingAssets/CityPack/` by glob and its
`ATTRIBUTION.json` 1 to 40. `tools/citypack/pack_check.py` by grep.
`tools/verdict-keys.py` 1 to 90. Queue 309, 310, 313 whole; D43, D45 whole;
the 07:55Z ruling whole; `rulings-log.md` whole; `organization.md` 90 to 150;
`verify.py` 3640 to 3710; NOW.md 1 to 120; `.claude/agent-log.tsv` 636 to 653.

Greps with their counts, all this session: the new tokens (`WetRedrive`,
`shotWetness`, `wetnessBind`, `wetnessNow`) over `ue-probe/`: 3 files, the
header, the .cpp and the test, no other; the old key names `wetnessValue` and
`wetnessFrom` over `tools/` and `ledger/verify.py`: 0 hits; `wetness` over
`.github/workflows/`: 0 hits (the probe workflow copies the verdict file whole
at yml 896 and 1042 to 1044 and greps no key); the twelve WATCHED tokens over
`THIRD-PARTY.md`: 37 occurrences on 31 lines, tabulated in section 8; `THIRD-
PARTY` and `assetId` over `pack_check.py`: 0 hits; `M17.6`, `surface set`,
`StreamingAssets/CityPack` over `THIRD-PARTY.md`: 5 hits, lines 181, 182, 184,
226, 300, none of them a row.

## 1. The answer in one paragraph

BOTH HALVES LAND, with the dictated corrections of section 11 applied by hand
before the commit and three queue items filed by number in the same commit
(322, 323, 324; texts in section 12). Nothing goes back to a builder. Of the
five deviations put to me: the per-shot key group is upheld and the item's own
acceptance sentence is ruled impossible on the file's structure (section 3);
the rename is upheld, for a reason the resident's instrument did not measure
(section 4); the route filter is correct, and its author's sentence about it
is stronger than the code it protects today (section 5); the three membership
keys are not a blocker and their comment was wrong in a way the re-drive
exposes, corrected by dictation, and beside them a printed token on the done
line says a false thing about the seed count and is corrected by one literal
(section 6); the walk over `GSpec.Pieces` is `BindSurfaces`' own idiom to the
character (section 7). The token fix in half B is complete; of twelve tokens
over seventeen rows, one is still satisfied by a path rather than a
declaration, it is `CityPack`, it predates this batch, and it sits over the
fifty-one shipped texture files whose row THIRD-PARTY.md has never carried
(section 8). The workflow never staged the `ATTRIBUTION.json` the new row
promises, and one dictated line fixes it. Queue 313's four-quadrant card is,
on the evidence of this batch's own header, very probably the A1(d) control
quad, and the cheapest decisive measurement is named for its taker (section
5). What this batch predicts for the run is written in section 10 before any
run exists.

## 2. What I verified, and where the brief to me was wrong

THE STRUCTURAL FACT UNDER DEVIATION 1, read off the file and not argued.
`WriteVerdict` (VignetteShot.cpp 2309) emits one line per `Bound` at 2514,
after every shot; `GBinds` holds one `Bound` per surface (613); `ReDriveWetness`
overwrites `GBinds[Idx].Wet`, `.Read.SetWet` and `.Read.GotWet` on every walk
(1695 to 1699, 1721 to 1724). So `midWetSetGot` on the surface line is the
last walk's pair by construction, and "one run shows midWetSetGot at 0.9000 on
a night shot and 0.0000 on wet_000" (queue 309 acceptance, line 23) asks one
key to carry two values on one line. The builder is right and the item was
wrong. The resident's D43 correction of 309 fixed the null-series sentence and
LEFT THIS ONE STANDING; section 11 dictates the second correction.

THE RESIDENT'S SAFETY CHECK ON THE RENAME MEASURED THE WRONG VERDICT. The
manifest `game-design/sim-shots/verdict-keys.json` belongs to
`tools/verdict-keys.py`, whose `VERDICT` is `game-design/sim-shots/verdict.txt`
(lines 51 to 52), the Unity sim's channel. The renamed keys live in
`production/d1-probe/ue-vignette-verdict.txt`, which that tool never reads, so
"neither renamed key is among its 1076" is true and proves nothing about this
rename. What proves the rename safe: `wetnessValue` and `wetnessFrom` have 0
hits in `tools/` and in `ledger/verify.py`; the two tools that do read the UE
verdict (`tools/map.py`, `tools/frame-shadow-probe.py`) are inside that zero;
and the probe workflow commits the verdict file whole with no key grep (0 hits
for `wetness` under `.github/workflows/`). The conclusion stands; the
instrument named for it does not.

"TEN SHOP SIGNS AND TEN POSTERS" IS TWENTY, AND THE FILTER PROTECTS TEN. Run
46's paint line (verdict 248) reads `paintRoutes=pack.580/tint.10/decal-
card.10/decal-multiply.0` with `decal-needs-a-stain-material.10`: ten card
pieces carry a dynamic instance (4031 to 4071) and the ten multiply pieces are
hidden before any instance is made (3971 to 3988). A walk without the filter
would cast the multiply pieces' parent material to null and count them `noMid`
(1666 to 1672); only the ten cards would be written. And what it would write
is section 5's finding.

"THE NULL GROUP GOES 9 TO 7 AND DISTINCT GROUPS 28 TO 30" IS RIGHT, checked
two ways. Spec lines 25 and 51 side by side: `overcast_day` and `wet_060` agree
on hdri, sun, lanterns, window_practicals, wetness 0.6, fog_density 0.012,
sun_intensity 3, sky_intensity 0.7, fog_max_opacity 0.1 and exposure_pin 0.3,
every field `AppliedFieldsUnreal` prints (VignetteSpec.h 2630). Run 46's null
line (verdict 325) reads `nullSeriesSamples=9/of=43`,
`nullSeriesTiedGroups=0/of=28`, ids `vign_hook_day;vign_grid_sky070_sun003;
vign_wet_000;vign_wet_060;vign_wet_100;vign_fog010_sky070;vign_pin_030_
afternight;vign_pin_030_afterday;vign_grid_null_repeat`. Remove the two rows
whose wetness differs and each becomes a singleton group: 7 and 30. The test
counts it off the discovered ids (4993 to 5011) rather than typing it, which is
the right shape.

WHAT I DID NOT RE-DERIVE AND SAY SO: the g++ counts (3382 from 3348), the
`-Wformat-truncation` figures at VignetteSpec.h 3111 (identical to HEAD's at
3095, moved by comment lines above the directive), `attribution-check.py`'s
exit 0 with rows 16 to 17 and assetFiles 2860 to 2856. Those are the
resident's printed measurements and are cited as such.

COMPILE-ORDER CHECKS THE CONTAINER CANNOT MAKE, made by reading: `GBinds` is
declared at 613 and `GWetRedrive` at 645, both above `ReDriveWetness` at 1598;
`FindCondition` is defined at 1553, above its new use at 3049. Every engine
call the walk makes is already in this file and already ran on run 46:
`GetStaticMeshComponent` (3949), `SetVectorParameterValue` and
`SetScalarParameterValue` (4115, 4125), `SetMaterial` (4128), a
`Comp->GetMaterial(0)` read (4379, in `SpawnControlQuads`), and
`K2_GetScalarParameterValue`, which the bind's own wetness readback uses and
run 46 proved answering with `midWetReadbackAll=14/14`. The only new call is
`Cast<UMaterialInstanceDynamic>` on a `UMaterialInterface*`, the engine's
ordinary cast. The risk is honest and small.

## 3. Call 1: the per-shot key group is the only evidence 309 can have, and it lands with its reading defined as a conjunction

THE ARGUMENT HOLDS. A surface line is last-wins (section 2). The done line's
`wetnessNow` is last-wins (SurfaceBind.h 1853, 1865). Neither can distinguish
"re-driven per condition" from "set once to the last condition's value", and
a batch whose whole claim is the former would land with no key able to refute
the latter. Only a per-sample key can, and instruments.md puts per-sample
numbers on the sample line under names the done line does not use. The group
`shotWetness`, `shotWetnessFrom`, `shotWetnessOnPieces`, `shotWetnessAgrees`,
`shotWetnessWalkedAt`, `shotWetnessStat` (1938 to 1963, appended at 3060) is
that shape, and `midWetSetGot` is correctly NOT reused on 43 lines (the file's
own naming law at 1571 to 1577, `midTilingReadback` against
`midScalarReadback`).

WHAT THE KEY CAN AND CANNOT PROVE, said here so the run is read right.
`shotWetnessOnPieces` is `GWetRedrive.LastWetness` (3054), which
`WetRedriveWalked` latches ON THE ASK, before the piece loop (1808 to 1815,
and the comment at 1800 to 1807 says so on purpose). So `shotWetnessAgrees=yes`
proves that the guard had latched this row's wetness before the shutter; it
does not prove any piece was written, and a walk whose every visit was refused
would still print `yes`. The run's evidence for 309 is therefore a CONJUNCTION
of three keys on two lines: `shotWetnessAgrees=yes` on the shot line, and on
the done line `wetnessRedriveWrote` with a numerator above zero on every walk
and `wetnessRedriveReadback=same-value`. The stat token on the shot line says
"onPieces-is-the-value-the-re-drive-last-latched" (1955), which is honest; the
key's NAME says a little more than the latch can carry. RULED: lands as built;
the reading order in section 10 puts the done line before the shot lines so
the hole cannot bite a reader; and the widening that closes it on the shot
line itself (the last walk's wrote/of carried per shot, and `shotWetnessFrom`
present on the nothing-measured shape too, which today omits it at 1942 to
1947 and so gives that shape a smaller key set than every other row) is queue
324, taken before the dusk frame is read. Not a blocker: the planted `Bad`
shape (3708 to 3719) and the planted wrote-nothing shape (3618 to 3641) cover
both failure directions across the two lines.

## 4. Call 2: the rename is right, and it is right for the reason this file already gives twice

`wetnessValue` meant the one value handed to every instance for the run and
photographed on every frame; after 309 the same number is a SEED that no frame
need be shot at. A key that keeps its name while its meaning moves is the fault
this very header names at `Bound` 1513 to 1519 ("would change what that number
means without changing its name, which is the quietest way there is to lose a
reading") and again at 1974 to 1983 beside the new name. Greppability across
the landed history is the argument FOR the rename, not against it: a reader
who greps `wetnessValue=` gets runs 45 and 46 and nothing after, and cannot
lay run 47's `wetnessBindValue=0.6000` beside run 46's `wetnessValue=0.6000`
and read them as the same measurement, because they are not. The test asserts
both old names ABSENT (3311 to 3315), so a run carrying both can never exist.
The cost is real and small: the 07:55Z ruling's section 9 and DISPATCH 1486
name the old key in their reading orders, and both describe run 46, which is
read (NOW.md, 22:10Z block). Nothing live reads the old names (section 2).
RULED: upheld. The next DISPATCH note names the new keys (section 10).

## 5. Call 3: the route filter is correct, its sentence overstates today, and 313's card is very probably the instrument

THE FILTER. `WetRedriveTouches` (1723 to 1726) admits `Paint_Pack` and
`Paint_Tint` and refuses the two decal routes and `Paint_None`. `BindSurfaces`
sets the AlbedoGrade and Wetness pair only on the instance made at 4074 to
4127, which is the pack and tint path; the decal-card instance at 4031 to 4062
sets the albedo texture, the shared card roughness and tiling 1 and NEITHER
parameter. `RouteFor` is re-run in the walk from the same three inputs the bind
used (1640 to 1642 against 3943 to 3945: `Pc.Surface`, `Pc.Shape == "decal"`,
`IsResolved(GBinds[Idx])`), so membership is recomputed, never stored, which
is the "no second list" the 07:55Z ruling ordered. The test examines all five
routes and plants the three refusals (3436 to 3461). CORRECT.

WHAT IT PROTECTS TODAY, measured rather than feared. Without the filter the ten
card instances would receive `WetGradeFor("card", false, W)` and
`WetBindFor("card", false, W)`. `AlbedoGradeFor` takes its decal branch at 638
to 642 and returns white; `WetBindFor` returns `bWet=false`, `Wetness=0.0` at
889 to 893 because `card` is not in `IsGroundSurface`; `WetGradeFor` returns
that white unchanged at 930. The material's AlbedoGrade default is white
(`make_base_material.py` 1396 asserts "must be white (1,1,1,1)") and the
Wetness default is 0.0 (SurfaceBind.h 2033, `WetnessDry()`). So the write the
filter prevents is a write of the card's own defaults: no pixel would move in
the judged frame today. The builder's sentence "would put an AlbedoGrade on ten
shop signs and ten posters in the judged frame" is therefore stronger than the
code, and this record says so, because a future reader must not learn from it
that the frame was ever at risk. The filter is still right and still
load-bearing, for two reasons that are true today: `wetnessRedriveWrote` must
count only pieces the bind set the pair on, or the tally would read 600 written
where the bind said 590 set; and the day `AlbedoGradeFor` learns to grade a
decal, or the default moves, the walk must already refuse. RULED: stays.

BEARING ON 313, in two halves. Through the walk: none. The three control quads
are `GQuadActors` (671), spawned by `SpawnControlQuads` and never in
`GSpec.Pieces`, so no walk visits them and no filter is needed to keep them
untouched. Through the reading: this batch's own header says what 313 is
looking at. `MakeControlTexture` (4249 to 4278) builds a 2x2 nearest-filtered
texture from `ControlColour` (2825 to 2830): red, green, blue, yellow, "four
flat quadrants". The controls stand in front of the camera the FIRST shot
uses (`ControlCamera`, 4284 to 4292; the first shot is `vign_camA_day`, spec
line 60, so the camera is `cam_A`), and `ControlQuadsVisibleFor` (2488 to
2493) shows them on every shot taken from that camera. `vign_camA_night` is a
`cam_A` shot (spec 61). Run 46's own verdict says so on the line: lines 189
and 190, the two `cam_A` shots, read `shotWholeFrameIncludesControlQuads=yes/
whole-frame-keys-on-this-line-include-them/...PROJECTED-BOXES-NOT-MEASURED-
COVERAGE`, and every other shot reads `no/hidden-for-this-camera`; line 273
reads `controlQuads=3/3` with `controlQuadsRule=the-colour-quad-answers-the-
texture-path/the-two-tile-quads-answer-the-scalar-path`. One four-colour card
and two tile quads of one texture at tiling 1 and 8, in the night frame, on the
control camera, is 313's "four-quadrant primary-colour card" and "two texture
swatches", and its measured appendix (the card present in three runs,
predating wetness) is consistent with an instrument that landed at A1(d).
This is a LEAD and not a diagnosis, under rule 4: the decisive measurement is
the projected box the colour quad's own line prints (`boxes=see-
controlQuadVisibility-and-the-quad-lines`) laid against 313's measured
rectangle (487,298)-(613,423). If they coincide, 313 closes as the instrument
by design, and the question that remains is the dusk frame's: Jafar's judged
picture must not be taken from the control camera with the controls shown, so
the dusk item either takes another camera or hides the controls for that shot
by name. That is 313's taker's to measure and the dusk item's to rule; it is
recorded here because the batch under review is where the evidence sits.

## 6. Call 4: the three keys are membership counts that cannot move, the comment beside them was wrong, and a printed token beside them says a false thing

THE THREE KEYS. `WetnessDoneSegment` counts `bWetSet`, `Wet.bWet` and
`Wet.bAlbedo` over `GBinds` (2006 to 2013). `bWetSet` is set at the bind
(4138) and re-set true by the walk on the same instances (1696), so the set
cannot grow: the walk requires the cast at 1666 to succeed, which the bind's
`Mid == nullptr` path (4075) makes impossible for any surface the bind skipped.
`Wet.bWet` is `IsGroundSurface(Surface)` and is set TRUE BEFORE the value is
looked at (894 against 895), so it is true at wetness 0.0 exactly as at 0.9.
`Wet.bAlbedo` is ground AND `bTextured` (896 to 901), and the walk hands the
same `bTextured` the bind recorded (`bAlbedoBound`, written at 4146, read at
1678). So all three are last-wins in FORM (the struct is rewritten every walk)
and invariant in VALUE, and the builder's "their values do not move with
wetness" is true. NOT A BLOCKER.

THE COMMENT WAS WRONG BEFORE THIS BATCH AND THE RE-DRIVE MAKES IT VISIBLE.
Line 1993 to 1994 says `wetnessSurfacesWet` counts surfaces that "took a
non-zero value". At the seed of 0.6 that happened to be true; after walk six
of the run (wet_000, section 10) the four ground surfaces carry 0.0000 with
`bWet` true, and the count still reads 4. instruments.md's first bullet is
satisfied by a comment beside the emit, so the correction is dictated as
comment text (section 11, item 1) and `wetnessSurfacesDarkened`, which the
list at 1985 to 2002 never documented, gets its line.

THE FALSE TOKEN, which nobody put to me and the diff put to me. The done line's
`wetnessStat` ends "...that-gap-is-NOT-a-compromise-only-the-shots-that-
needed-no-re-drive" (2026 to 2027), and the comment at 2000 to 2002 says
`wetnessShotsAtValue` "says now only how many shots never needed a re-drive".
That is not what the guard does. The guard is keyed on the LAST applied value
(1790 to 1794), so `vign_camB_day` at 0.6 after `vign_camB_night` at 0.9
needs a walk although its condition carries the seed value. On the committed
file 35 shots carry the seed and 26 need no walk (section 10 counts the 17
walks). A printed token that names one number and describes another is the
first bullet of instruments.md broken on the done line of every future run.
RULED: one literal corrected by dictation (section 11, item 2), shorter than
the text it replaces by design so the 700-byte buffer at 2014 is not
approached, the comment made to match, and the tail of the segment asserted
present by one dictated `Check` in block 13 of the test so the done line can
never be cut silently at exactly that key (section 11, item 4).

## 7. Call 5: the walk is `BindSurfaces`' own idiom, to the character

`GByName` is `TMap<FString, AStaticMeshActor*>` (510): a name to an actor, no
surface. `WetBindFor`'s membership is `IsGroundSurface(Surface)`. The bind's
own piece loop at 3927 to 3950 does exactly what the walk does at 1622 to
1653: `for P over GSpec.Pieces`, find the bind record by `Pc.Surface`,
`RouteFor(Pc.Surface, Pc.Shape == "decal", IsResolved(...))`, then
`GByName.Find(FString(UTF8_TO_TCHAR(Pc.Name.c_str())))` (3947 against 1648,
identical text). The lit-window pass at 1330 resolves names through the same
`Find`. The claim is verified against `BindSurfaces` and not accepted. The
departure from the ruling's wording ("walk GByName") is the right departure,
the comment at 1582 to 1591 names it, and this record confirms it so the next
reader does not re-litigate it.

## 8. Half B where it touches the licence record: the token fix is complete, one older hazard is measured, the pack has no row, and the workflow never staged the JSON

THE FIX IS COMPLETE. `citypack_shortlist` occurs ONCE in `THIRD-PARTY.md`, on
its own heading (246), and nowhere else; the hyphenated form occurs three
times, all inside the paragraph (264 to 265, 278) and the workflow path that
explain why it was rejected, and the tool's `token in text` (493, 509, 518,
530) is case-sensitive and literal, so the hyphen and the underscore are
different strings. The rejecting case that found the fault was run by hand on
the live document, which is the only place it COULD be found: the selftest's
`full_doc` (692 to 696) writes the tokens joined by spaces and nothing else,
so no rejecting fixture in the suite has ever seen a path, a filename or
another row's prose. That is why this class of fault will be found by hand
again unless 322 is done, and 322 is filed.

BOTH COUNTS, RULE 3b. Twelve distinct tokens over seventeen WATCHED rows were
examined against all 331 lines of `THIRD-PARTY.md`, 37 occurrences on 31 lines.
Nine tokens occur only inside their own section (Mixamo 72 to 81; VCTK 20 to
66; The Base Mesh 187, 191; OpenGameArt 198, 202; vignette-surfaces 228;
citypack_shortlist 246; Poly Haven 286, 290; Rockstar Games 323, 326; SIL Open
Font License 112). Three are satisfied by text OUTSIDE their own section:
`Kenney` by the OpenGameArt section's prose (209 three times, 212); `ambientCG`
by the vignette-surfaces section (228, 232) and, since this batch, by the
shortlist section (246, 250, 251, 258, 283); `CityPack` by the path
`StreamingAssets/CityPack` at 184 inside the LOG paragraph headed "Textures,
props, vehicles, NOTHING YET", and, since this batch, by the cross-reference
at 251. Of the exact class the builder found, a token that is a SUBSTRING OF A
PATH named in the document: one of twelve, `CityPack`, pre-existing. Four
near-misses were checked so nobody re-checks them, and none matches the
case-sensitive test: `polyhaven` in the path at 292 against `Poly Haven`; `SIL
OPEN FONT LICENCE` at 105 and `SIL Open Font Licence` at 164 against `SIL Open
Font License`; `the Base Mesh row` at 211 against `The Base Mesh`;
`vignette/surfaces` in the path at 234 against `vignette-surfaces`.

THE PACK HAS NO ROW, AND THE RECORD IS THE PART THAT HAS TO BE RIGHT. The
watched path `ledger/Assets/StreamingAssets/CityPack` (attribution-check 95)
holds 51 texture files under `textures/` (17 surfaces, three maps each, by
glob this session) and an `ATTRIBUTION.json` naming ambientCG and CC0 1.0
Universal per surface. `THIRD-PARTY.md` has no section for it: the only
occurrences of the token are the path at 184 and the new sentence at 251, and
the sentence at 251 says "the same source and licence as the CityPack, decal
and vignette rows above" about a CityPack row that does not exist. The JSON's
own first line says "THIRD-PARTY.md is the human-readable copy and both must
agree; tools/citypack/pack_check.py enforces it", and `pack_check.py` has zero
hits for `THIRD-PARTY` and zero for `assetId`, so no tool enforces it. Nothing
unlicensed ships, ambientCG is line 5 of the allowlist, and the machine record
is complete; what is missing is the human-readable row the project's own
document calls the obligation (THIRD-PARTY.md 9 to 11, 127, 214 to 215).
Beside it, one stale cell: the Decals row's Where (223) names
`ledger/Assets/Decals/ambientcg/<AssetID>/`, which does not exist by glob,
while the watched path is `ledger/Assets/StreamingAssets/Decals` (159), which
does and carries its own THIRD-PARTY.md inside. Neither was caused by this
batch; the batch touched the first (251). RULED: the false sentence at 251 is
corrected by dictation now (section 11, item 6) so the document stops claiming
a row it does not have, and the row itself, the Decals cell and the false
enforcement claim are queue 323, a document correction under D43 that needs
seventeen asset ids transcribed and is a builder's minute rather than a
director's dictation.

THE WORKFLOW NEVER STAGES THE JSON THE ROW PROMISES. THIRD-PARTY.md 252 (and
the tool's own row comment at 104 to 106) say `ATTRIBUTION.json` is "written
by the same run that writes the pixels" under `tools/citypack/shortlist/`.
`fetch_textures.py` does write it there, on every call and even on an empty
run (606 to 634, called at 680, 687 and 767 with `out_root = HERE /
"shortlist"` at 706), which is the right shape under ci.md. The workflow's
commit step stages exactly two paths by name, `tools/citypack/shortlist/
contact-*.png` and `tools/citypack/shortlist-results.json` (118 to 119), and
the JSON is under neither. The artifact carries it (162); git never will. On
the first live run the contact sheets land and the record beside them does
not, and the row's Where cell is false on the day it first matters. RULED: one
line added by dictation in the file's own two-calls idiom (section 11, item
7). The four deleted PNGs are gone by glob, NOTE.txt's account of them (each a
sheet of one already-shipped, already-attributed tile) is consistent with the
pack listing, and nothing a person spent time on was among them, so rule 5 is
satisfied.

## 9. The resident's 309 correction, checked

Correct on the null series (section 2) and incomplete on the acceptance line:
"one run shows midWetSetGot at 0.9000 on a night shot and 0.0000 on wet_000"
still stands at line 23 and is the impossible ask (section 3). Section 11,
item 8 dictates its replacement and the status line.

## 10. What this batch predicts for the run, written before the run exists

WHICH RUN. The first landed run whose commit CONTAINS this batch's commit,
settled by ancestry and never by run number or branch movement (ci.md). No
re-dispatch of anything queued; run 46 is read and is the comparison.

THE WALK COUNT, DERIVED FROM THE SHOT ORDER (spec 60 to 102) AND THE GUARD
(keyed on the last applied value, first application always walks). Wetness by
shot: 0.6, 0.9, 0.6, 0.9, 0.6, then 0.6 for shots 6 to 21, then 0.0 (wet_000),
0.6 (wet_060), 1.0 (wet_100), 0.6 for shots 25 to 30, then 0.9, 0.6, 0.6, 0.9,
0.6, 0.6, 0.9, 0.6, 0.6, 0.9, 0.6, 0.6, 0.6, and the determinism repeat of shot
1 at 0.6. Walks happen at shots 1, 2, 3, 4, 5, 22, 23, 24, 25, 31, 32, 34, 35,
37, 38, 40, 41: SEVENTEEN. The last walk is for condition `pin_1000` at 0.6;
shots 42, 43 and the repeat skip. Per walk the visits are the file's 610
pieces (run 46: `piecesEmitted=610/610`), of which pack 580 and tint 10 are
written and card 10 and multiply 10 are refused as not our route; no piece has
an empty surface, no actor is missing, no component is missing, and every pack
or tint piece carries the instance the bind made (`compMaterialIsMid=14/14`).

THE DONE LINE, materials, appended after run 46's keys:
`wetnessNow=0.6000 wetnessNowFrom=pin_1000`;
`wetnessRedriveWalks=17/of=N/...` and `wetnessRedriveSkipped=(N-17)/of=N/...`
where N is the SAME number the scene line prints as `skyWrites=44/of=N`
(both count `ApplyCondition` calls: 1755 feeds 1962, and `WetRedriveAsked` is
called once per `ApplyCondition` at 1600 via 1878; run 46's N was 4162);
`wetnessRedriveWrote=10030/of=10370/piece-visits...` (590 times 17 over 610
times 17); `wetnessRedriveRefused=notOurRoute.340/noBind.0/noActor.0/
noComponent.0/noMid.0`; `wetnessRedriveReadback=same-value
wetnessRedriveSetGot=0.6000..0.6000` (the readback is the last walk's first
written piece, `ground_east_carriageway`, asphalt, spec 105);
`wetnessRedriveTallyMismatch` ABSENT and `wetnessRedriveReadNote` ABSENT;
`wetnessBindValue=0.6000 wetnessBindFrom=overcast_day/first-shot-condition
wetnessSurfacesSet=14/16 wetnessSurfacesWet=4/14 wetnessSurfacesDarkened=4/14
wetnessShotsAtValue=35/43 wetnessCondsAtValue=29/33`, all unchanged from run
46 but for the two names; `midWetReadbackAll=14/14`.

THE SHOT LINES, all 43: `shotWetnessAgrees=yes`. The eight rows at another
wetness read `shotWetness=0.9000 shotWetnessOnPieces=0.9000` (the six night
rows), `0.0000..0.0000` (wet_000) and `1.0000..1.0000` (wet_100), each with
`shotWetnessWalkedAt` equal to its own condition. The skip shape appears on
rows 6 to 21 (`shotWetnessWalkedAt=overcast_day`), 26 to 30
(`fog_maxop0020`), 33 (`pin_003`), 36 (`pin_030`), 39 (`pin_300`), and 42 and
43 (`pin_1000`, with `shotWetnessFrom=grid_null_repeat` on 43), and it is not
a fault.

THE SURFACE LINES: asphalt, kerb, sidewalk, concrete read `wetSet=0.6000
wetSetFrom=pin_1000 midWetSetGot=0.6000..0.6000`; the ten other pack and tint
surfaces read `wetSet=0.0000 wetSetFrom=pin_1000 midWetSetGot=0.0000..0.0000`;
card and multiply read `wetSet=not-set` with `wetSetFrom=bind-time-seed/no-
condition-was-applied-after-it`, which is the pre-existing not-set shape and
reads oddly on a card that never had a seed either; noted, not a fault.

THE NULL SERIES: `nullSeriesSamples=7/of=43`, `nullSeriesTiedGroups=0/of=30`,
ids `vign_hook_day;vign_grid_sky070_sun003;vign_wet_060;vign_fog010_sky070;
vign_pin_030_afternight;vign_pin_030_afterday;vign_grid_null_repeat`,
`nullSeriesApplied` carrying `/wet0.6000`, `nullSeriesExcludes=none/...`.

THE CONTROLS THAT MUST NOT MOVE, from DISPATCH 1508 to 1511: `band.skyCentre.
p50` at 0.8035 and `shotExposurePinRead` 0.3000/0.3000; this batch writes two
material parameters and nothing else.

THE FRAMES, in rule-2 terms and with no number. Run 46 measured the wet_000
against wet_060 pair INSIDE the null floor (NOW.md: 76.03 per cent of pixels
at maxChannelDelta 41/255 against the certified null pair's 75.59 per cent at
29/255). This batch predicts that `vign_wet_000` against `vign_wet_100`
separates BEYOND the null pair on the same two statistics, and that
`vign_camA_night`'s road reads darker and more reflective than run 46's under
the same camera. If the keys above all hold and the frames do not separate,
the write reached the game thread's copy and not the render proxy, which the
comment at 1705 to 1707 names as the one thing the readback cannot see; the
control quads are the render side of that question.

WHAT IS A FAULT, named now. `wetnessRedriveWrote` with a zero numerator on
any run that printed walks above zero is the walk finding nothing it may
write, and the refusal buckets say why. `wetnessRedriveReadback=DIFFERENT` is
the dead write. `shotWetnessAgrees=NO` on any row is a frame photographed at a
wetness the guard had not latched, and the row prints both numbers and the
walk it did latch. `wetnessRedriveTallyMismatch` present is a counter that
moved without its twin. Walks other than 17 on the committed file is either a
changed shot order or the guard keyed on the wrong thing, and the second is
what test 15d plants (3535 to 3544).

## 11. Dictated corrections, exact text, applied by hand before the commit

Comment and literal text only, plus one `Check` line in the test's own idiom,
one workflow line in the file's own idiom, and D43 document corrections. After
items 2 and 4 the resident re-runs the g++ suite: the count goes 3382 to 3383,
and the printed line `wetness done segment: N of 700 buffer byte(s)` must
show N at or below 680 (my hand count of the live shape after item 2 is about
641, and the worst shape this function can build, `pin_setter_night` with the
longest `Why`, is 18 bytes longer). If either goes red or N exceeds 680, the
batch HOLDS and goes back to the engine builder with the printed number.

1. `SurfaceBind.h`, replace lines 1991 to 1994 (the four comment lines from
   `wetnessSurfacesSet` to `a non-zero value.`) with:

       //   wetnessSurfacesSet  surfaces whose instances carry the scalar, over
       //                       surfaces the shared file asked for. MEMBERSHIP
       //                       and not a value: the bind sets it on the pack
       //                       and tint routes and the re-drive rewrites the
       //                       same set on every walk, so it is last-wins in
       //                       form and cannot move in value. A run in which it
       //                       moves is a run in which the walk reached a
       //                       surface the bind did not, and that is the finding.
       //   wetnessSurfacesWet  of those, how many are in WetSurfaces. Membership
       //                       again: bWet is IsGroundSurface and is true at
       //                       wetness 0.0 too, so this reads 4 after wet_000
       //                       and after wet_100 alike and never says what value
       //                       the four hold. wetnessNow and the shot lines do.
       //   wetnessSurfacesDarkened  of those, how many bound an albedo and so
       //                       took the colour half as well as the roughness
       //                       half. Membership, off bAlbedoBound recorded at
       //                       the bind and handed to the walk unchanged.

2. `SurfaceBind.h` line 2027, the literal
   `"compromise-only-the-shots-that-needed-no-re-drive",` becomes
   `"compromise-shots-at-the-seed-value-not-shots-that-skipped-a-walk",`

3. `SurfaceBind.h`, replace lines 2000 to 2002 (from `photographed at its own.
   What the gap says now is` to `wetnessRedriveWalks is the reading that
   matters.`) with:

       //                       photographed at its own. The gap is NOT how many
       //                       shots skipped a walk: the guard is keyed on the
       //                       LAST applied value, so a seed-value shot after a
       //                       night row walks. On the committed file 35 shots
       //                       carry the seed and 26 skip. wetnessRedriveWalks
       //                       is the reading that matters.

4. `vignette-spec-test.cpp`, block 13, insert after line 3337 (the line ending
   `"the wetness segment is key=value throughout", Seg2);`):

       				Check(Seg2.find("not-shots-that-skipped-a-walk") != std::string::npos,
       				      "the done segment reaches its last token, so the 700-byte "
       				      "buffer that carries it did not truncate", Seg2);

   and at 3334 to 3335 the message `"and the count of shots that need no
   re-drive at all is a number on the line rather than a thing a reader
   infers"` becomes `"and the count of shots at the seed value is a number on
   the line, and it is not a count of walks"`.

5. `production/queue/310-...md`, append to the spec field one sentence:
   "Queue 309 added two more: WetRedriveSegment 860, whose worst shape the
   suite already builds and asserts (test 15d, the pattern to copy), and
   WetShotFields 420, of which two shapes are printed and the worst (two
   sixteen-character condition ids) is not built."

6. `THIRD-PARTY.md` line 251, replace `the same source and licence as the
   CityPack, decal and vignette rows above` with `the same source and licence
   as the decal and vignette-surfaces rows above and as the shipped pack under
   `ledger/Assets/StreamingAssets/CityPack/`, whose own row this file still
   owes (queue 323)`.

7. `.github/workflows/citypack-shortlist.yml`, after line 119 insert:

             git add tools/citypack/shortlist/ATTRIBUTION.json 2>/dev/null || true

   and in the comment block above it, after the sentence ending `costs two
   lines.` (117), add: `# THREE, SINCE THE 22:25Z RULING: ATTRIBUTION.json is
   the record the THIRD-PARTY.md row promises beside the pixels, and a commit
   of the sheets without it makes that row false on the first live run.`

8. `production/queue/309-...md`: in the acceptance field, `one run shows
   midWetSetGot at 0.9000 on a night shot and 0.0000 on wet_000` becomes `one
   run shows shotWetness and shotWetnessOnPieces at 0.9000 with
   shotWetnessAgrees=yes on a night row and at 0.0000 on wet_000, on the two
   rows' own lines (midWetSetGot is one surface's last-wins pair and cannot
   carry two values in one run, ruled 22:25Z)`; and the status field gains
   `LANDED 2026-09-15 under the 22:25Z ruling, UNRENDERED; the run that
   carries it is the first landed run whose commit contains the batch commit,
   read in that ruling's section 10 order.`

## 12. Three queue items, filed in the same commit, field form per `tools/queue-check.py`

QUEUE: 322 the-attribution-token-is-satisfied-by-a-path-and-the-fixture-cannot-see-it

`production/queue/322-the-attribution-token-is-satisfied-by-a-path-and-the-fixture-cannot-see-it.md`:

    line: gate (tools/attribution-check.py, its selftest)
    spec: The row check is `token in text` over the whole of THIRD-PARTY.md
      (486 to 533 on 2026-09-15). A token that is a substring of a path,
      a filename or another row's prose passes with its own declaration
      deleted. Found by hand on 2026-09-15 for citypack-shortlist against
      the workflow filename and fixed by respelling; measured the same
      night by the 22:25Z ruling over all twelve tokens: CityPack is
      satisfied only by the path at THIRD-PARTY.md 184 inside a LOG
      paragraph headed NOTHING YET, and Kenney and ambientCG are satisfied
      by other rows' sections. The selftest cannot see any of this: its
      full_doc (692 to 696) is the tokens joined by spaces, so no
      rejecting fixture has ever contained a path or a sentence.
    acceptance: a token counts only on a DECLARATION line (a `## ` heading
      carrying it, or a table row whose first cell is Source, Licence or
      Where), never in prose or a path; the rejecting fixture is built
      from the LIVE THIRD-PARTY.md by blanking one row's declaration lines
      and asserting that row alone goes red, run for every WATCHED row so
      the count of rows examined prints beside the count refused; the
      accepting fixture is the live document, which requires 323 first.
      Both counts on the done line. Under D45: a test, no review.
    max_sessions: 1
    status: READY 2026-09-15, filed by the 22:25Z ruling. Ordered after 323,
      because the accepting fixture is the live document and the live
      document is red under this rule until the pack has its row.

QUEUE: 323 the-shipped-pack-has-no-row-and-the-json-says-a-tool-enforces-one

`production/queue/323-the-shipped-pack-has-no-row-and-the-json-says-a-tool-enforces-one.md`:

    line: licence record (THIRD-PARTY.md, ledger/Assets/StreamingAssets/
      CityPack/ATTRIBUTION.json, tools/citypack/pack_check.py)
    spec: ledger/Assets/StreamingAssets/CityPack/textures/ holds 51 files,
      17 surfaces by three maps, ambientCG CC0 1.0 Universal per the
      directory's ATTRIBUTION.json, and THIRD-PARTY.md carries no row for
      it: the token occurs only as the path at 184 and the cross-reference
      at 251 (corrected 2026-09-15 to stop claiming a row). The JSON's
      first line says pack_check.py enforces agreement with THIRD-PARTY.md;
      pack_check.py has no reference to THIRD-PARTY.md and none to
      assetId. The Decals row's Where (223) names
      ledger/Assets/Decals/ambientcg/, which does not exist; the watched
      and populated path is ledger/Assets/StreamingAssets/Decals/.
      Nothing unlicensed ships (ambientCG is allowlist line 5); the
      record is what is wrong, and THIRD-PARTY.md's own words at 127 and
      214 say the record is the part that has to be right. D43: a
      correction, applied and reported, not carded.
    acceptance: a section for the shipped pack in the shape of the
      vignette-surfaces row, naming source, licence, Where, What and every
      assetId the JSON names, with a parenthetical token on the heading;
      the Decals Where corrected; either pack_check.py compares the JSON's
      ids against THIRD-PARTY.md with both counts printed, or the JSON's
      sentence is corrected to name what does enforce it. Reported in the
      day's brief as what was wrong and is now right.
    max_sessions: 1
    status: READY 2026-09-15, filed by the 22:25Z ruling. Before 322.

QUEUE: 324 the-per-shot-wetness-line-can-read-clean-when-the-walk-wrote-nothing

`production/queue/324-the-per-shot-wetness-line-can-read-clean-when-the-walk-wrote-nothing.md`:

    line: engine (SurfaceBind.h WetShotIn and WetShotFields,
      VignetteShot.cpp 3046 to 3061, vignette-spec-test.cpp 15g)
    spec: shotWetnessOnPieces is the guard's latch, taken on the ask
      before the piece loop (WetRedriveWalked, latched on purpose so a
      scene with nothing writable is not re-walked per tick). So
      shotWetnessAgrees=yes proves the latch and not a write, and a walk
      whose every visit was refused prints yes on every frame after it.
      Today the done line's wetnessRedriveWrote is the only key that can
      say so, and the 22:25Z ruling defines 309's evidence as the
      conjunction of the two lines. Also the nothing-measured shape
      (1942 to 1947) omits shotWetnessFrom, so that shape has a smaller
      key set than every other row; the surface line's own test at 3773
      to 3777 already refuses that for wetSetFrom.
    acceptance: the shot line carries the last walk's wrote over visits
      (for example shotWetnessWalkWrote=590/of=610), and shotWetnessAgrees
      reads NO-WALK-WROTE-NOTHING when that numerator is zero, with the
      planted case in 15g; shotWetnessFrom present on every shape; buffer
      series printed before the 420 is touched (queue 310's pattern).
      No new key on the done line; a widening of an existing line, not an
      instrument.
    max_sessions: 1
    status: READY 2026-09-15, filed by the 22:25Z ruling. Taken before the
      dusk frame is read, because that is the frame a false yes would cost.

## 13. For NOW.md and the rulings log

NOW.md, one paragraph in the resident's words to this effect: both halves of
the 22:25Z batch are committed under this ruling and the wetness half is
UNRENDERED; the reading order for the run that carries it is section 10 of
this record and the DISPATCH note for that run quotes section 10's predictions
verbatim, dated, so the run can refute them; 322, 323 and 324 are filed;
313's taker has a lead in section 5 and the decisive measurement named;
`citypack-shortlist.yml` is dispatched only after item 7 of section 11 is in
the tree, because the first live run must commit the JSON beside the sheets.

`ledger-v2/respec/decision-register/rulings-log.md`, appended in date order:

    - **2026-09-15** per-condition wetness lands (309): the per-shot key
      group is the only evidence the item can have and its own acceptance
      sentence was an impossible ask; the rename to wetnessBindValue and
      wetnessBindFrom is upheld; the route filter is correct and protects
      ten card instances from a write of their own defaults; the three
      membership keys cannot move and their comment is corrected; a done-
      line token that called the seed count a walk count is corrected;
      the walk over GSpec.Pieces is BindSurfaces' own idiom; 313's card is
      very probably the A1(d) control quad; the shortlist token fix is
      complete and one of twelve tokens is still satisfied by a path,
      CityPack, over a shipped pack with no row (323); the workflow never
      staged ATTRIBUTION.json, one line dictated; 322 and 324 filed;
      predictions for the run written before it exists
      `game-design/decision-2026-09-15-ruling-per-condition-wetness-lands-and-the-licence-record-owes-the-pack-its-row.md`

## 14. The quality ladder at close

Wetness: the next rung named by the 07:55Z ruling is taken, per condition
through the components the scene already kept, with a guard that the suite
runs as a loop and a per-frame key that says which wetness each frame wore.
Best available for what the item asked. The rung after it is unchanged from
the 07:55Z record: whether the frame needs standing water is Jafar's reading
of the first wet frame, filed at 186's close by name if his reading says the
reflection is not there. The instrument's own next rung is 324.

The licence record: first working, and named as such. A token test that reads
prose is the rung the studio has been on since the file was written; the rung
above it is a declaration-line test with a rejecting fixture built from the
live document, and it is 322, behind the row it needs first, 323.

The process: two things worth one line each. A director without a diff can
still rule from the tree when the changed regions are found by their new
tokens, and it is slower and it is complete; and a safety check should be
made on the artifact the change touches, which for a verdict key means the
verdict that carries it and not the manifest of a different one.

## 15. For the commit message

Queue 309, per-condition wetness: `ApplyCondition` re-drives `Wetness` and
`AlbedoGrade` on the instances the scene already keeps, through `WetBindFor`
and `WetGradeFor` with the bind's own inputs, on the pack and tint routes
only, with a write-on-change guard keyed on the last applied value whose loop
the suite runs (120 asks, 3 walks, 117 refused, 1200 writes against 48000).
Wetness joins the null-series fingerprint; wet_000 and wet_100 leave the
cam_hook group and wet_060 stays because it IS the reference cell's wetness
(9 to 7, 28 to 30, counted off the ids). Per-frame keys `shotWetness*` on the
shot line; `wetnessValue`/`wetnessFrom` renamed `wetnessBindValue`/
`wetnessBindFrom` because the number became a seed, old names asserted
absent. NOTHING RENDERED. g++ 3383 after one dictated `Check`, generator
selftest unchanged, as printed by the resident. Citypack attribution: the
WATCHED token respelled `citypack_shortlist` after the rejecting case found
the hyphenated one satisfied by the workflow's filename; four dead-run PNGs
removed; the shortlist workflow now stages `ATTRIBUTION.json` beside the
sheets. Queues 322, 323, 324 filed by this ruling. Jafar's 0.85 untouched.

<!--RULING spawn=2026-09-15T22:25:05Z-->
